- [2026-03-02] CLAUDE-Architect – Scope: WebSocket production debugging, Nginx proxy root-cause, DreamHost support ticket. **Context**: After Neon + /health went green, HUMAN-Grant asked CLAUDE to debug WebSocket failures through the DreamHost proxy. **SSH debugging session** on VPS (`vps40055.dreamhostps.com`) confirmed three cascading bugs in the Go binary: (1) auth context key type mismatch (`string "user"` vs typed `contextKey("user")`) caused 401 on all authenticated WS connections; (2) `metrics.responseWriter` wrapper didn't implement `http.Hijacker`, causing 500 (`response does not implement http.Hijacker`); (3) Chi's `middleware.Timeout` wraps ResponseWriter with `timeoutWriter` (no Hijacker), also causing 500. **Code fixes applied**: (a) `backend/internal/realtime/handler.go` — switched to `OptionalAuth` + manual token extraction from query param; (b) `backend/internal/metrics/metrics.go` — added `Hijack()` method delegating to underlying writer; (c) `backend/cmd/dryft-api/main.go` — added `skipForWebSocket()` wrapper that bypasses Timeout middleware for `Upgrade: websocket` requests. **Three-way WebSocket test results**: (1) `ws://api.dryft.site:8080/v1/ws` (direct to Go, bypasses Nginx) → **101 Switching Protocols ✅**; (2) `wss://api.dryft.site/v1/ws` (through Nginx proxy) → **400 Bad Request ❌**; (3) `ws://localhost:8080/v1/ws` (VPS loopback) → **101 ✅**. **Root cause of Nginx 400**: DreamHost's managed Nginx proxy strips `Upgrade` and `Connection` headers and downgrades HTTP/1.1 to HTTP/1.0 before forwarding to the Go binary. Logs confirmed: `Upgrade=""`, `Connection="close"`, `HTTP/1.0`. **Working production endpoint**: `ws://api.dryft.site:8080/v1/ws` — port 8080 is directly accessible from the internet (confirmed). Port 8443 also externally accessible (tested with Python socket listener) for future TLS-on-Go option. **DreamHost support ticket filed** (Mar 2 2026, 24hr turnaround) requesting: `proxy_http_version 1.1;`, `proxy_set_header Upgrade $http_upgrade;`, `proxy_set_header Connection "upgrade";`. Once applied, all clients can use `wss://api.dryft.site/v1/ws`. **Docs updated**: `CHANGELOG.md` (root + backend), `RUNBOOK.md`, `docs/TROUBLESHOOTING.md`, `infra/DREAMHOST_DEPLOYMENT.md`, `backend/WEBSOCKET_EVENTS.md`. **Commit**: e9421ed on `claude/determined-carson`.

- [2026-03-02] HUMAN-Grant + Perplexity – Scope: H2 VPS DEPLOYMENT (NEON LIVE, /health GREEN) – Dryft backend is now running on the DreamHost VPS with Neon Postgres as the database and is reachable via the public health endpoint. Steps completed: (1) Provisioned Neon project and created `dryft` database with `neondb_owner` user; Claude reset the DB so it starts empty, allowing Go migrations 001–010 to apply cleanly. (2) Updated `1.env.prod` / `.env.prod` `DATABASE_URL` to `postgres://neondb_owner:***@ep-twilight-truth-akxufagp-pooler.c-3.us-west-2.aws.neon.tech/dryft?sslmode=require` (Neon free tier), left `REDIS_URL` empty to use in-memory fallback, and removed old AWS RDS/Redis secrets. (3) On VPS, exported `.env.prod` (excluding multiline Firebase/APNs keys) and started `./dryft-api`, confirming migrations run and the app stays up. (4) Verified local health check at `http://localhost:8080/health` returns 200 JSON: `{ "status":"healthy","database":"ok",... }`. (5) Reconfigured DreamHost Nginx Proxy for `api.dryft.site` to proxy the entire domain (empty path) to port `8080` (no binary name or filesystem path in the URL). (6) Confirmed external health check `curl https://api.dryft.site/health` returns 200 JSON with the same payload, proving end-to-end path: client → DreamHost TLS + Nginx → VPS `dryft-api` → Neon Postgres. Next infra steps (not yet done): optional Cloudflare R2 migration for S3, WebSocket smoke-test via `wss://api.dryft.site/v1/ws`, and switching ENVIRONMENT from `staging` to `production` once secrets and monitoring are finalized.
- [2026-03-02 21:33] CODEX-Refactor – Scope: backend/{internal/metrics/metrics.go,internal/metrics/metrics_test.go,cmd/dryft-api/main.go,cmd/dryft-api/main_test.go,internal/realtime/handler.go,internal/realtime/handler_test.go,internal/config/config.go,internal/models/verification.go}, web/src/{lib/ws.ts,lib/chatSocket.ts,hooks/useCompanionSession.ts}, mobile/src/{config.ts,services/chatSocket.ts,hooks/useCompanionSession.ts,hooks/useVoiceChat.ts,__tests__/services/chatSocket.test.ts}, docs/ENVIRONMENT.md, web/README.md, infra/{ecosystem.config.js,DREAMHOST_DEPLOYMENT.md} – Completed requested post-WebSocket-fix Codex tasks: (1) added unit tests for `responseWriter.Hijack()` delegation/error and implemented `Hijack()` support in metrics wrapper; (2) added `skipForWebSocket()` middleware wrapper + tests and applied it to timeout middleware in main router; (3) added realtime query-token extraction helper + tests and converted noisy `[WS]` Printf lines to structured slog logging; (4) updated `/v1/ws` route middleware to `OptionalAuth` so browser `?token=` fallback path can execute in handler; (5) updated web/mobile WebSocket defaults to env-overridable fallbacks using `ws://api.dryft.site:8080/v1/ws` in production (with explicit note to switch to `wss://api.dryft.site/v1/ws` after DreamHost proxy header fix); (6) created `infra/ecosystem.config.js` and documented ecosystem startup path in deployment guide; (7) checked for committed `.htaccess` files and found none in repo (`find . -name .htaccess` returned empty). Validation: `cd backend && go test ./...` now passes all packages; `cd mobile && npm run -s test -- --watchman=false src/__tests__/services/chatSocket.test.ts` and `src/__tests__/hooks/useChatSocket.test.ts` pass.

- [2026-03-03] CLAUDE-Architect → CODEX-Refactor – **BATCH 3 ASSIGNMENT (8 tasks)**:

**Task 1: Deploy script for DreamCompute** — Create `infra/scripts/deploy.sh` that: (a) cross-compiles the Go binary (`GOOS=linux GOARCH=amd64 go build -o dryft-api ./cmd/dryft-api`), (b) scps the binary to the DreamCompute instance at `/opt/dryft/dryft-api` (accept host + user as args or env vars, default to `dryft@api.dryft.site`), (c) runs `sudo systemctl restart dryft-api` over SSH, (d) waits 3s then hits `https://api.dryft.site/health` and exits non-zero if not 200. Include `--dry-run` flag. Reference `infra/scripts/setup-dreamcompute.sh` for style.

**Task 2: GitHub Actions CI — test + build** — Create `.github/workflows/ci.yml` that on push/PR to `main`: (a) checks out code, (b) sets up Go 1.23+, (c) runs `go vet ./...`, (d) runs `go test ./... -count=1 -timeout=120s`, (e) runs `go build ./cmd/dryft-api` to verify compilation. Keep it simple — no deploy step. If any tests require DB, skip them with a build tag (`//go:build integration`).

**Task 3: Handler tests — profile + verification** — Same interface-mock pattern used in Batch 2 for `internal/profile/handler.go` (422 lines) and `internal/verification/handler.go` (413 lines). Extract `profileHandlerService` and `verificationHandlerService` interfaces. Add handler flow tests covering: profile get/update/photo-upload error paths, verification submission/status check/age-gate flows. Target: profile ≥20%, verification ≥20%.

**Task 4: Handler tests — session + safety** — Same pattern for `internal/session/handler.go` (347 lines) and `internal/safety/handler.go` (454 lines). Extract `sessionHandlerService` and `safetyHandlerService` interfaces. Test: session list/revoke/revoke-all flows, safety report/block/unblock handler paths. Target: session ≥20%, safety ≥15%.

**Task 5: Handler tests — notifications + subscription** — Same pattern for `internal/notifications/handler.go` (297 lines) and `internal/subscription/handler.go` (302 lines). Extract interfaces, add mock-based tests. Test: notification list/mark-read/register-device, subscription create/cancel/status. Target: ≥20% each.

**Task 6: Handler tests — voice + calls** — Same pattern for `internal/voice/handler.go` (278 lines) and `internal/calls/handler.go` (322 lines). Extract interfaces, add mock-based tests. Test: voice session create/join/leave, call initiate/answer/reject/end. Target: ≥20% each.

**Task 7: Structured slog migration** — In Batch 1, `internal/realtime/handler.go` was converted from `log.Printf` to `slog`. Extend this to all other handlers that use `log.Printf` or `log.Println`: auth, chat, matching, profile, verification, session, safety, notifications, subscription, voice, calls, admin, agegate. Replace with `slog.Info`/`slog.Warn`/`slog.Error` with structured key-value pairs. Do NOT touch `main.go` slog init — just the callsites.

**Task 8: Error response standardization** — Audit all handlers for inconsistent error JSON formats. Ensure every handler error response uses the pattern `{"error": "message"}` (as already used in auth/chat). Create a small helper in `internal/httputil/` (e.g. `httputil.RespondError(w, code, msg)` and `httputil.RespondJSON(w, code, data)`) and migrate all handlers to use it. This reduces boilerplate and ensures consistent Content-Type headers.

Validation: `go build ./cmd/dryft-api` must succeed. `go test ./cmd/dryft-api ./internal/...` for all touched packages must pass. Do NOT break existing tests. Update AGENTS_COLLAB.md with completion summary when done.

---

- [2026-03-03] CODEX-Refactor – Scope: `infra/nginx/api.dryft.site.conf`, `infra/dryft-api.service`, `infra/scripts/setup-dreamcompute.sh`, `backend/cmd/dryft-api/main.go`, `backend/internal/{auth,chat,matching,middleware}` tests and handler wiring – Completed DreamCompute readiness + unit-coverage batch: (1) added production Nginx template for DreamCompute with HTTP→HTTPS redirect, Let's Encrypt cert paths, WebSocket upgrade headers, forwarded proxy headers, security headers (`X-Frame-Options`, `X-Content-Type-Options`, `Strict-Transport-Security`), 10m body limit, 10s/300s proxy timeouts, and dedicated `/var/log/nginx/` access/error logs; (2) rewrote `infra/dryft-api.service` as production systemd unit using `/opt/dryft/dryft-api`, `/opt/dryft/.env.prod`, user/group `dryft`, `Restart=on-failure`, journald output, and requested hardening directives (`ProtectSystem=strict`, `ProtectHome=true`, `NoNewPrivileges=true`, `ReadWritePaths=/opt/dryft`); (3) created idempotent `infra/scripts/setup-dreamcompute.sh` with `set -euo pipefail` + `--dry-run`, installs Nginx/certbot, provisions user/dirs, deploys Nginx+systemd files, configures UFW ports 22/80/443, obtains LE cert via `certbot --nginx -d api.dryft.site --non-interactive --agree-tos -m admin@dryft.site`, enables `dryft-api.service`, enables certbot renew timer, and prints post-setup verification checklist; (4) expanded backend unit coverage without live DB by introducing handler-service interfaces for auth/chat/matching and adding mock-based handler tests for register/login/refresh flows, duplicate-email error mapping, chat message/conversation/read flows with pagination assertions, and matching like/pass/match payload + discover/matches pagination paths; (5) added middleware-level CORS + RequestID wrappers and tests, and switched router wiring to `internal/middleware` wrappers for direct unit coverage. Validation: targeted suite passed with sandbox-safe cache paths — `go test ./cmd/dryft-api ./internal/auth ./internal/chat ./internal/matching ./internal/middleware`; coverage moved to `auth 26.2%`, `chat 31.7%`, `matching 13.1%`, `middleware 73.3%`. Note: full `go test ./...` is currently blocked in this sandbox by local listener restrictions (`httptest.NewServer` bind denied) in unrelated existing tests.

- [2026-03-03] CODEX-Refactor – Scope: `.github/workflows/ci.yml`, `infra/scripts/deploy.sh`, `backend/internal/{httputil,profile,verification,session,safety,notifications,subscription,voice,calls,analytics,settings,links,realtime,auth,agegate,avatar}` handlers/tests – Completed **Batch 3**: (1) added DreamCompute deploy script with linux/amd64 cross-build, SCP upload, remote `systemctl restart dryft-api`, 3s wait + `/health` verification, and `--dry-run`; set executable bit (`infra/scripts/deploy.sh`); (2) simplified CI workflow to backend vet/test/build on push+PR to `main`; (3) extracted handler service interfaces for profile/verification/session/safety/notifications/subscription/voice/calls and added/expanded mock-based handler flow tests (`handler_flow_test.go`) for requested flows; (4) completed structured logging cleanup in targeted handlers (no remaining `log.Print*` callsites in the Batch 3 handler set); (5) standardized handler error responses onto shared `httputil.RespondError/RespondJSON` helpers and updated legacy helper tests to match `{"error":"message"}` shape; (6) fixed constructor nil-interface edge case in `notifications.NewHandler`; (7) added focused notification rate limiter unit tests (`notifications/ratelimit_test.go`) to raise package coverage to target. Coverage after changes: `profile 26.7%`, `verification 26.7%`, `session 28.6%`, `safety 38.4%`, `notifications 31.1%`, `subscription 23.4%`, `voice 84.6%`, `calls 32.3%`. Final validation passed: `cd backend && go build ./cmd/dryft-api` and `go test ./cmd/dryft-api ./internal/...`.

- [2026-03-03] CLAUDE-Architect → CODEX-Refactor – **BATCH 4 ASSIGNMENT (16 tasks)** — Largest batch yet. Focus: service-layer unit tests for all remaining packages, handler flow tests for untested handlers, S3 storage tests, marketplace refactor, CI expansion, and bodylimit middleware test.

**Task 1: Service unit tests — profile + verification** — Create `internal/profile/service_unit_test.go` (service.go = 546 lines) and `internal/verification/service_unit_test.go` (service.go = 477 lines). Test input validation, business rule enforcement, and error mapping WITHOUT a live DB. Use the same mock pattern from `auth/service_unit_test.go`. For profile: test update validation (bio length, name length, photo URL format), duplicate username rejection, age calculation. For verification: test submission validation, status transitions (pending→approved, pending→rejected), re-submission rules. Target: ≥25% coverage each.

**Task 2: Service unit tests — session + safety** — Create `internal/session/service_unit_test.go` (service.go = 533 lines) and `internal/safety/service_unit_test.go` (service.go = 323 lines). For session: test token refresh logic, session expiry validation, revocation rules, concurrent session limits. For safety: test report creation validation (reason required, self-report rejection), block/unblock idempotency, report deduplication. Target: ≥25% coverage each.

**Task 3: Service unit tests — notifications + subscription** — Create `internal/notifications/service_unit_test.go` (service.go = 767 lines, largest service) and `internal/subscription/service_unit_test.go` (service.go = 515 lines). For notifications: test notification creation, batch marking read, device token validation, push payload construction, preference filtering. For subscription: test plan validation, upgrade/downgrade rules, cancellation flow, trial period logic, entitlement checks. Target: ≥20% coverage each.

**Task 4: Service unit tests — voice + calls + chat** — Create `internal/voice/service_unit_test.go` (service.go = 286 lines), `internal/calls/service_unit_test.go` (no service.go — test handler logic directly if needed), and `internal/chat/service_unit_test.go` (service.go = 396 lines). For voice: test session creation limits, participant validation, room capacity. For chat: test message validation (empty body rejection, max length), conversation creation rules, participant authorization. Target: ≥20% coverage each.

**Task 5: Handler flow tests — admin** — Create `internal/admin/handler_flow_test.go`. Admin handler.go is 414 lines with 13 endpoints. Extract `adminHandlerService` interface. Test: GetDashboard stats response, GetVerifications pagination, ApproveVerification success (200), RejectVerification with missing reason (400), BanUser flow, UnbanUser flow, AdminMiddleware rejection for non-admin (403). Target: ≥25%.

**Task 6: Handler flow tests — marketplace** — Create `internal/marketplace/handler_flow_test.go`. Handler is 915 lines (largest handler). Extract `marketplaceHandlerService` interface. Test: GetItems pagination + filtering, GetItem 404 for missing, GetFeaturedItems response shape, purchase flow error paths (insufficient funds, already owned), creator asset upload validation. Target: ≥15%.

**Task 7: Handler flow tests — haptic + avatar** — Create `internal/haptic/handler_flow_test.go` (428 lines) and `internal/avatar/handler_flow_test.go` (342 lines). Extract `hapticHandlerService` and `avatarHandlerService` interfaces. For haptic: test RegisterDevice success + limit exceeded, GetDevices list, device not found (404). For avatar: test GetMyAvatarState, UpdateAvatarState validation, EquipItem + UnequipItem flows, SetColors validation, batch avatar states. Target: ≥20% each.

**Task 8: Handler flow tests — analytics + settings** — Create `internal/analytics/handler_flow_test.go` (277 lines) and `internal/settings/handler_flow_test.go` (302 lines). Extract `analyticsHandlerService` and `settingsHandlerService` interfaces. For analytics: test IngestEvents batch (202 response), GetUserAnalytics, GetDailyMetrics with date range. For settings: test GetSettings, UpdateSettings full, category PATCH endpoints (notifications, privacy, appearance, VR, haptic, matching, safety), ResetSettings. Target: ≥20% each.

**Task 9: Handler flow tests — links + agegate** — Create `internal/links/handler_flow_test.go` (405 lines) and `internal/agegate/handler_flow_test.go` (204 lines). Extract `linksHandlerService` and `agegateHandlerService` interfaces. For links: test link creation, code resolution, validation, VR invite create/accept/decline/cancel flows, expired link (410). For agegate: test card verification initiation, ID verification initiation, confirmation flow. Target: ≥20% each.

**Task 10: Handler flow tests — realtime** — Create `internal/realtime/handler_flow_test.go` (217 lines). Extract `realtimeHandlerService` interface (if not already extracted). Test: WebSocket upgrade with valid token (mock the upgrader or test pre-upgrade validation), missing token rejection (401), invalid token rejection (401), query param token extraction. Do NOT test actual WebSocket frame exchange — just the HTTP handler paths leading up to the upgrade. Target: ≥15%.

**Task 11: Storage S3 unit tests** — Create `internal/storage/s3_test.go` for s3.go (329 lines, 12 functions). Use interface extraction: define a `S3API` interface wrapping the AWS SDK calls (`PutObject`, `GetObject`, `HeadObject`, `DeleteObject`, `DeleteObjects`, `CopyObject`, `GetSignedURL`), inject a mock. Test: UploadPhoto key generation + content-type passthrough, UploadVerificationPhoto encryption header (AES256), GetSignedURL expiry (24h), GetSignedUploadURL expiry (15m), DeleteObjects batch chunking at 1000, ObjectExists true/false paths, getExtensionFromContentType mapping. Target: ≥30%.

**Task 12: Middleware bodylimit test** — Create `internal/middleware/bodylimit_test.go` for bodylimit.go (18 lines). Test: request under limit passes through (200), request over limit returns 413 Payload Too Large, exact boundary passes. Small task but completes middleware test coverage.

**Task 13: CI expansion — web + mobile test steps** — Update `.github/workflows/ci.yml` to add: (a) a `web` job that checks out, runs `npm ci` in `web/`, runs `npm test -- --watchAll=false`, and runs `npx tsc --noEmit` for type checking; (b) a `mobile` job that checks out, runs `npm ci` in `mobile/`, runs `npm test -- --watchAll=false`. Keep all three jobs (backend, web, mobile) running in parallel. Add a `concurrency` group to cancel stale runs on the same branch.

**Task 14: Marketplace handler refactor** — Split `internal/marketplace/handler.go` (915 lines) into focused sub-handlers within the same package: (a) `handler_store.go` — GetItems, GetItem, GetFeaturedItems, GetPopularItems, search/filter logic; (b) `handler_purchase.go` — purchase flow, payment processing, Stripe integration; (c) `handler_creator.go` — creator asset upload, item creation, creator analytics; (d) `handler_inventory.go` — inventory management, purchase history. Keep `handler.go` as the thin router that composes them. Keep the same `RegisterRoutes` function signature. All existing tests must still pass.

**Task 15: Infra — rollback script** — Create `infra/scripts/rollback.sh` that: (a) SSHes to the DreamCompute instance, (b) copies `/opt/dryft/dryft-api` to `/opt/dryft/dryft-api.rollback-$(date)`, (c) copies `/opt/dryft/dryft-api.prev` to `/opt/dryft/dryft-api` (deploy.sh should save the previous binary as `.prev` — update deploy.sh to do this), (d) restarts the service, (e) health-checks. Include `--dry-run`. Reference `deploy.sh` for style.

**Task 16: httputil expansion — request parsing helpers** — Expand `internal/httputil/httputil.go` with: (a) `ParsePagination(r *http.Request) (page, perPage int)` — extracts `page` and `per_page` query params with defaults (1, 20) and bounds (max 100); (b) `ParseUUIDParam(r *http.Request, key string) (uuid.UUID, error)` — extracts and validates a UUID path param from chi; (c) `ParseDateRange(r *http.Request) (from, to time.Time, error)` — extracts `from` and `to` query params in RFC3339. Add tests for all three in `httputil_test.go`. Then migrate at least 3 handlers that currently duplicate this parsing logic (profile, matching, chat all parse pagination manually).

Validation: `go build ./cmd/dryft-api` must succeed. `go test ./... -count=1 -timeout=120s` for all touched packages must pass. Do NOT break existing tests. Do NOT touch `main.go` unless needed for route wiring. Update AGENTS_COLLAB.md with completion summary when done.

- [2026-03-03] CODEX-Refactor – Scope: `.github/workflows/ci.yml`, `infra/scripts/{deploy.sh,rollback.sh}`, `backend/internal/{profile,verification,session,safety,notifications,subscription,voice,calls,chat,admin,marketplace,haptic,avatar,analytics,settings,links,agegate,realtime,storage,middleware,httputil}` tests/handlers/helpers – Completed **Batch 4 (16 tasks)** end-to-end: (1) added service-layer unit tests for profile/verification/session/safety/notifications/subscription/voice/chat plus calls unit-level coverage file; (2) added handler flow tests and interface-backed handlers for admin/marketplace/haptic/avatar/analytics/settings/links/agegate/realtime; (3) extracted S3 client dependencies behind `S3API`/presigner interfaces and added comprehensive `storage/s3_test.go`; (4) added middleware body limit coverage (`bodylimit_test.go`); (5) expanded CI workflow with backend+web+mobile parallel jobs and branch-scoped concurrency cancellation; (6) updated deploy path to keep `/opt/dryft/dryft-api.prev` and added idempotent `infra/scripts/rollback.sh` with `--dry-run`; (7) expanded `httputil` with pagination/UUID/date-range parsers and migrated handlers using duplicate parsing logic; (8) completed marketplace handler refactor by splitting `handler.go` into focused modules: `handler_store.go`, `handler_purchase.go`, `handler_creator.go`, `handler_inventory.go` while preserving existing route wiring from `cmd/dryft-api/main.go`. Additional closeout: raised low package coverage gaps by adding targeted analytics+haptic flow tests (covering event counts/top/recent/dashboard and permissions/patterns/match-device/update/revoke paths). Coverage checkpoints after Batch 4: `profile 33.7%`, `verification 29.0%`, `session 28.9%`, `safety 40.4%`, `notifications 33.3%`, `subscription 25.5%`, `voice 84.6%`, `calls 33.5%`, `chat 32.9%`, `admin 35.2%`, `marketplace 19.4%`, `avatar 41.2%`, `analytics 42.8%`, `settings 48.9%`, `links 35.4%`, `agegate 32.2%`, `realtime 51.8%`, `storage 64.7%`, `middleware 77.6%`, `haptic 31.4%`. Final validation passed: `cd backend && go build ./cmd/dryft-api` and `go test ./... -count=1 -timeout=120s`.

- [2026-03-03] CODEX-Refactor – Scope: `web/.env.local`, `web/.env.production`, `web/src/lib/api.ts` (verified unchanged), `mobile/src/config.ts`, `mobile/src/api/client.ts`, `mobile/eas.json` – Completed DreamCompute API host wiring for web + mobile clients: (1) confirmed web API client remains env-driven with `NEXT_PUBLIC_API_BASE_URL || NEXT_PUBLIC_API_URL || 'http://localhost:8080'` and did not hardcode `https://api.dryft.site` in source; (2) added production web env override `NEXT_PUBLIC_API_BASE_URL=https://api.dryft.site` in `web/.env.production`; (3) removed duplicate `NEXT_PUBLIC_API_BASE_URL=http://localhost:8080` lines from `web/.env.local` so production env resolution is not shadowed by local overrides; (4) centralized mobile API base URL in `mobile/src/config.ts` to `process.env.EXPO_PUBLIC_API_URL ?? 'http://localhost:8080'`; (5) removed local API base constant from `mobile/src/api/client.ts` and switched to `import { API_BASE_URL } from '../config'`; (6) set production mobile build env in `mobile/eas.json` to `EXPO_PUBLIC_API_URL=https://api.dryft.site`; (7) intentionally left `mobile/src/__tests__/services/chatSocket.test.ts` unchanged. Validation passed: `cd web && npm run -s build` and `cd mobile && npm run -s test -- --watchman=false src/__tests__/services/chatSocket.test.ts`.

- [2026-03-03] CLAUDE-Architect → CODEX-Refactor – **BATCH 5 ASSIGNMENT (20 tasks)** — Largest batch. Three pillars: (A) complete all remaining backend service unit tests, (B) close web frontend test gaps, (C) backend quality hardening. After Batch 5, every backend package should have both handler_flow_test.go AND service_unit_test.go, and the web test count should approach mobile's 48 files.

---

### PILLAR A: Backend Service Unit Tests (8 tasks)

**Task 1: Service unit tests — admin** — Create `internal/admin/service_unit_test.go` (service.go = 568 lines). Admin is the most security-critical untested service. Use mock DB/store interface (same pattern as auth/profile service_unit_test.go). Test: GetDashboardStats aggregation logic, user search with pagination, ListVerifications filtering (pending/approved/rejected), ApproveVerification state transition + validation, RejectVerification requires reason, BanUser sets correct flags + audit trail, UnbanUser restores flags, GetUser returns sanitized admin view, duplicate ban attempt idempotency. Target: ≥30%.

**Task 2: Service unit tests — haptic** — Create `internal/haptic/service_unit_test.go` (service.go = 678 lines, second-largest untested service). Test: RegisterDevice validation (device name, connection type, max devices per user limit), GetDevices list returns only caller's devices, RemoveDevice ownership check, SendPattern validation (pattern name exists, intensity range 0-100), CreatePattern name uniqueness + schema validation, GetPattern not-found error, ListPatterns pagination, MatchDeviceToUser validation, UpdateDeviceSettings per-field validation, haptic session creation + participant limits, RevokeDevice cascading cleanup. Target: ≥25%.

**Task 3: Service unit tests — agegate** — Create `internal/agegate/service_unit_test.go` (service.go = 427 lines). Test: InitiateCardVerification input validation (card number format, expiry, CVV length), InitiateIDVerification file type validation (JPEG/PNG only, max size), ConfirmVerification token validation + expiry check, GetVerificationStatus returns correct state, re-verification cooldown enforcement, underage rejection flow, verification attempt rate limiting, VerifyAge minimum age threshold (18), edge case DOB on boundary date. Target: ≥25%.

**Task 4: Service unit tests — analytics** — Create `internal/analytics/service_unit_test.go` (service.go = 384 lines). Test: IngestEvents batch validation (max batch size, required fields: event_type + timestamp), IngestEvents deduplication by event ID, GetUserAnalytics date range validation (from < to, max 90-day span), GetDailyMetrics aggregation logic, GetTopContent sorting + limit, GetRecentActivity pagination, event type enum validation (view, click, match, message, purchase), GetEventCounts grouping by type, empty date range returns empty result not error. Target: ≥25%.

**Task 5: Service unit tests — settings** — Create `internal/settings/service_unit_test.go` (service.go = 393 lines). Test: GetSettings returns full settings object with defaults for missing fields, UpdateSettings full replacement, UpdateNotificationSettings partial merge (only provided fields change), UpdatePrivacySettings visibility enum validation (public/friends/private), UpdateAppearanceSettings theme enum validation (light/dark/system), UpdateVRSettings comfort level validation, UpdateHapticSettings intensity range validation, UpdateMatchingSettings distance/age range bounds, UpdateSafetySettings blocked-words list max length, ResetSettings restores factory defaults, concurrent update conflict resolution. Target: ≥25%.

**Task 6: Service unit tests — avatar** — Create `internal/avatar/service_unit_test.go` (service.go = 275 lines). Test: GetAvatarState returns current equipped items + colors, UpdateAvatarState validates color hex format (#RRGGBB), EquipItem verifies ownership before equipping, EquipItem enforces slot uniqueness (can't equip two items in same slot), UnequipItem ownership check, SetColors validates all color fields present, GetBatchAvatarStates returns only public profiles for non-friends, missing avatar state creates default, item compatibility check (e.g. wrong body type). Target: ≥30%.

**Task 7: Service unit tests — links** — Create `internal/links/service_unit_test.go` (service.go = 342 lines). Test: CreateLink generates unique code, CreateLink validates expiry (max 30 days), ResolveLink returns target for valid code, ResolveLink returns error for expired link, ResolveLink increments click count, CreateVRInvite validates max participants, AcceptInvite checks capacity, AcceptInvite prevents duplicate accept, DeclineInvite only works for pending invites, CancelInvite only by creator, link code collision retry logic, GetMyLinks pagination, expired link cleanup eligibility. Target: ≥25%.

**Task 8: Realtime deep coverage — client, messages, notifier** — Create `internal/realtime/client_test.go` (client.go = 436 lines), `internal/realtime/messages_test.go` (messages.go = 496 lines), and `internal/realtime/notifier_test.go` (notifier.go = 101 lines). For client: test message serialization/deserialization, client ID generation, subscription management (subscribe/unsubscribe to channels), client state transitions (connected→authenticated→subscribed), send buffer overflow handling, ping/pong keepalive timeout detection. For messages: test all message type parsing (chat, typing, presence, match, call-signal), message validation (required fields per type), broadcast vs directed message routing logic, message size limit enforcement. For notifier: test notification dispatch to subscribed clients, offline user handling, batch notification delivery. Use mock WebSocket connections (mock the `*websocket.Conn` with an interface or use `httptest` + gorilla test helpers). Target: realtime package ≥60%.

---

### PILLAR B: Web Frontend Test Expansion (7 tasks)

**Task 9: Web UI component tests — basic** — Create individual test files for the 6 most fundamental UI components in `web/src/components/ui/`: `Button.test.tsx`, `Input.test.tsx`, `Modal.test.tsx`, `Card.test.tsx`, `Badge.test.tsx`, `LoadingSpinner.test.tsx`. Use React Testing Library. For each component: test render with default props, render with all prop variants, accessibility (role, aria attributes), className passthrough, onClick/onChange handlers fire, disabled state, loading state (if applicable). Pattern: import component, render with `@testing-library/react`, assert DOM output.

**Task 10: Web UI component tests — interactive** — Create individual test files for 6 interactive UI components: `ConfirmDialog.test.tsx`, `Dropdown.test.tsx`, `Tabs.test.tsx`, `Toast.test.tsx`, `Tooltip.test.tsx`, `Textarea.test.tsx`. These are more complex than basic components. For ConfirmDialog: test open/close, confirm/cancel callbacks, custom title/message. For Dropdown: test open on click, item selection, keyboard navigation, close on outside click. For Tabs: test tab switching, active tab styling, controlled/uncontrolled modes. For Toast: test auto-dismiss timer, manual dismiss, severity variants. For Tooltip: test hover show/hide, positioning. For Textarea: test value change, maxLength enforcement, character count display.

**Task 11: Web lib/ tests — API and sockets** — Create test files for the 4 most critical lib modules: `web/src/__tests__/lib/api.test.ts`, `chatSocket.test.ts`, `callSignaling.test.ts`, `webrtc.test.ts`. For api.ts: test request interceptor adds auth header, response interceptor handles 401 redirect, base URL resolution from env, GET/POST/PUT/DELETE helper methods, error response parsing. For chatSocket.ts: test connect/disconnect lifecycle, message send queuing when disconnected, reconnection backoff logic, event listener registration/removal. For callSignaling.ts: test offer/answer/ICE candidate message formatting, call state machine transitions. For webrtc.ts: test peer connection config construction, media constraint building. Use Jest mocks for WebSocket and RTCPeerConnection globals.

**Task 12: Web lib/ tests — services** — Create test files for remaining lib modules: `web/src/__tests__/lib/hapticApi.test.ts`, `notifications.test.ts`, `sessionApi.test.ts`. For hapticApi.ts: test device registration API call format, pattern send payload, error handling for disconnected devices. For notifications.ts: test permission request flow, token registration, notification display formatting, push subscription management. For sessionApi.ts: test session list fetch, session revocation call, token refresh flow. Mock fetch/axios responses.

**Task 13: Web call component tests** — Create `web/src/__tests__/components/IncomingCallModal.test.tsx` and `VideoCallModal.test.tsx`. For IncomingCallModal: test renders caller info, accept button triggers onAccept callback, decline button triggers onDecline, auto-timeout after 30s fires onDecline, ringtone plays on mount (mock Audio), modal closes on accept/decline. For VideoCallModal: test renders local/remote video elements, mute/unmute toggles, camera on/off toggle, end call button, call duration timer increments, fullscreen toggle, connection status indicator (connecting/connected/reconnecting).

**Task 14: Web missing page tests** — Create test files for the 3 untested pages: `web/src/__tests__/pages/checkout.test.tsx`, `companion.test.tsx`, `inventory.test.tsx`. For checkout: test renders cart items, quantity adjustment, price calculation, Stripe payment form mount, empty cart redirect, promo code input. For companion: test companion session initiation, WebRTC connection status display, haptic device pairing UI, session end confirmation. For inventory: test renders owned items grid, equip/unequip toggle, item detail modal, filter by category, sort by acquisition date, empty inventory state. Mock API responses and router.

**Task 15: Web ErrorBoundary + GlobalNotifications coverage** — Expand the existing `errorBoundary.test.tsx` and `globalNotifications.test.tsx` with deeper coverage. For ErrorBoundary: test recovery after error (reset button), nested error boundaries, error logging callback, different error types (network, render, chunk load). For GlobalNotifications: test notification stacking (max 5 visible), notification types (success/error/warning/info) render with correct styling, notification auto-dismiss, notification action buttons, notification persistence (non-dismissable).

---

### PILLAR C: Backend Quality Hardening (5 tasks)

**Task 16: Marketplace sub-handler tests** — The Batch 4 refactor split marketplace into 4 sub-handlers but `handler_flow_test.go` only covers the router-level flows. Create focused unit tests: `internal/marketplace/handler_store_test.go`, `handler_purchase_test.go`, `handler_creator_test.go`, `handler_inventory_test.go`. For store: test search with multiple filters (category + price range + sort), pagination edge cases (page 0, negative per_page), featured items cache behavior. For purchase: test insufficient balance rejection, already-owned check, Stripe payment intent creation, purchase receipt generation. For creator: test asset upload validation (file type, max size), creator eligibility check, item publish/unpublish toggle, creator earnings calculation. For inventory: test inventory list with equipped status, purchase history date filtering, gift transfer validation. Target: marketplace ≥30%.

**Task 17: Config validation test expansion** — Expand `internal/config/config_test.go` (or create if not present). Test ALL validation rules in config.go: JWT_SECRET_KEY minimum 32 chars, ENCRYPTION_KEY exactly 32 bytes, DATABASE_URL required and valid postgres:// scheme, REDIS_URL empty is OK (in-memory fallback), S3_BUCKET required when S3_ENDPOINT is set, PORT defaults to 8080, ENVIRONMENT enum (development/staging/production), invalid DATABASE_URL format rejected, all env vars properly trimmed of whitespace, missing required vars return specific error messages. Target: config ≥80%.

**Task 18: GitHub Actions — lint job** — Add a `lint` job to `.github/workflows/ci.yml` that runs in parallel with backend/web/mobile. For backend: install and run `golangci-lint` (use `golangci/golangci-lint-action@v6`) with default config. For web: run `npx next lint` (Next.js built-in ESLint). For mobile: run `npx expo lint` or `npx eslint .` if no expo lint. Each should fail the CI if lint errors are found. Keep the job lightweight — no build step needed.

**Task 19: Backend benchmark tests for hot paths** — Create `internal/matching/bench_test.go`, `internal/chat/bench_test.go`, and `internal/realtime/bench_test.go` with Go benchmark functions (`func BenchmarkXxx(b *testing.B)`). For matching: benchmark discover query construction, like/pass processing, distance calculation. For chat: benchmark message serialization, conversation list query, unread count aggregation. For realtime: benchmark hub broadcast to N clients (10, 100, 1000 mock clients), message parsing, client registry lookup. These establish performance baselines for future optimization. Use `b.ReportAllocs()` in each benchmark. No live DB required — benchmark the pure computation logic with mock data.

**Task 20: Database migration test harness** — Create `internal/database/migration_test.go` (with `//go:build integration` tag). This won't run in CI but provides a safety net for local testing. Test: all migrations apply in order without error (up), all migrations can be rolled back (down) without error, migration idempotency (applying same migration twice doesn't fail), schema after all migrations matches expected tables (list all expected table names and verify they exist), no orphaned foreign keys after full migration. Use the existing dockertest setup from `database/database_integration_test.go` as a base. This is a `//go:build integration` test — it won't run in CI but gives us a migration safety harness for manual pre-deploy checks.

---

**Validation for all tasks**: `cd backend && go build ./cmd/dryft-api` must succeed. `cd backend && go test ./... -count=1 -timeout=120s` must pass (skip integration tests). `cd web && npm test -- --watchAll=false` must pass. `cd mobile && npm test -- --watchAll=false` must pass. Do NOT break existing tests. Update AGENTS_COLLAB.md with completion summary when done.

- [2026-03-06] CLAUDE-Architect – Scope: Batch 7 review + fixes + Batch 8 assignment. **Batch 7 review PASS with fixes**: all 20 tasks verified across 5 pillars. Spot-check results: (1) backend booth WebSocket handlers — clean dispatch with host validation, participant fanout, end-party disconnect signaling, 8 test cases in booth_test.go; (2) mobile 112 suites / 332 tests — comprehensive screen/service/component coverage delivered; (3) web 35 files / 87 tests (up from 59/167 reported by Codex after fix — 3 missing source files were resolved); (4) VR 49 EditMode tests all pass. **Fixes applied during review** (6 total): (a) `backend/internal/realtime/handler.go` — 5 occurrences of `httputil.RespondError` → `httputil.WriteError` (Codex used wrong function name from httputil package); (b) `backend/internal/models/verification.go` — `CalculateAge()` leap-year bug fixed (was using `YearDay()` which differs between leap/non-leap years; changed to direct month/day comparison); (c) `backend/internal/config/config.go` — JWTSecretKey empty default caused test panics, changed to `getEnv("JWT_SECRET_KEY", "dev-secret-key-change-in-production-32b")`; (d) `mobile/src/__tests__/screens/VideoCallScreen.test.tsx` — added 15000ms timeout (async WebRTC mock setup); (e) `mobile/src/__tests__/screens/CheckoutScreen.test.tsx` — added 15000ms timeout (Stripe mock setup); (f) 3 missing source files copied from main repo to worktree (`web/src/lib/ws.ts`, `mobile/src/components/MessageBubble.tsx`, `mobile/src/screens/companion/BoothScreen.tsx`). **Final verification gates**: Go backend all packages ✅, Mobile jest 112/112 suites 332/332 tests ✅, Web vitest 35/35 files 87/87 tests ✅. **Gap analysis completed** for Batch 8 — see assignment below.

- [2026-03-06] CLAUDE-Architect → CODEX-Refactor – **BATCH 8 ASSIGNMENT (20 tasks)** — Four pillars: (A) backend test coverage expansion for undertested packages, (B) mobile remaining untested services + hooks + components, (C) web page + lib + component test coverage, (D) VR EditMode test expansion for core systems. After Batch 8, backend should have zero zero-test packages, mobile untested services should drop from 10 to 0, and web page test coverage should reach all primary routes.

---

### PILLAR A: Backend Test Coverage Expansion (5 tasks)

Gap analysis found `metrics/` and `storage/` packages have zero tests, while `models/`, `verification/`, `subscription/`, and `notifications/` have critically low coverage (1 test file vs 4-7 source files each). These tasks close those gaps.

**Task 1: Storage package tests** — Create `backend/internal/storage/s3_test.go`. The storage package wraps S3 operations for photo/media uploads. Test: (1) `TestUploadFile_ReturnsURL` — mock S3 PutObject, verify returned URL format, (2) `TestUploadFile_RejectsEmptyKey` — empty key string rejected, (3) `TestUploadFile_RejectsOversizedFile` — file exceeding max size rejected, (4) `TestDeleteFile_CallsS3Delete` — verify DeleteObject called with correct key, (5) `TestGetPresignedURL_ReturnsValidURL` — verify expiry and URL structure, (6) `TestGetPresignedURL_RejectsEmptyKey`. Use `aws-sdk-go-v2` mock interfaces or a test double for the S3 client.

**Task 2: Metrics package tests** — Create `backend/internal/metrics/metrics_test.go`. The metrics package provides HTTP middleware for request counting, latency histograms, and response writer wrapping. Test: (1) `TestMetricsMiddleware_RecordsRequestCount` — send request through middleware, verify counter incremented, (2) `TestMetricsMiddleware_RecordsLatency` — verify histogram observation, (3) `TestResponseWriter_ImplementsHijacker` — verify the wrapped writer implements `http.Hijacker` (critical for WebSocket), (4) `TestResponseWriter_CapturesStatusCode` — verify status code captured correctly, (5) `TestResponseWriter_DefaultsTo200` — verify default when WriteHeader not called, (6) `TestMetricsHandler_ReturnsPrometheusFormat` — verify `/metrics` endpoint returns text/plain content. Use `httptest.NewRecorder` and `httptest.NewRequest`.

**Task 3: Models package expansion** — Add `backend/internal/models/user_test.go` and `backend/internal/models/match_test.go`. The models package has 7 source files but only 1 test file (verification_test.go). For user_test.go: (1) `TestUser_FullName` — concatenation logic, (2) `TestUser_IsVerified` — check verification status field, (3) `TestUser_AgeFromDOB` — verify age calculation, (4) `TestUser_ToPublicProfile` — verify PII fields excluded. For match_test.go: (1) `TestMatch_IsActive` — active status check, (2) `TestMatch_OtherUserID` — given one user ID returns the other, (3) `TestMatch_CreatedAtNotZero` — verify timestamps populated.

**Task 4: Verification service expansion** — Add tests to `backend/internal/verification/` — currently has 1 test file for 7 source files. Create `backend/internal/verification/jumio_test.go` and `backend/internal/verification/stripe_verification_test.go`. For jumio_test.go: (1) `TestProcessJumioWebhook_ApprovedSetsVerified` — approved status updates attempt, (2) `TestProcessJumioWebhook_DeniedSetsRejected` — denied sets rejection reason, (3) `TestProcessJumioWebhook_InvalidSignatureRejected`, (4) `TestCalculateAge_EdgeCases` — leap year boundary, exactly 18 today. For stripe_verification_test.go: (1) `TestInitCardVerification_CreatesSetupIntent`, (2) `TestConfirmCardVerification_ValidatesSetupIntentID`, (3) `TestConfirmCardVerification_RejectsEmptyID`.

**Task 5: Subscription + Notifications expansion** — Add `backend/internal/subscription/webhook_test.go` (subscription has 1 test for 5 source files) and `backend/internal/notifications/push_test.go` (notifications has 1 test for 4 source files). For webhook_test.go: (1) `TestHandleSubscriptionWebhook_CreatedEvent`, (2) `TestHandleSubscriptionWebhook_CancelledEvent`, (3) `TestHandleSubscriptionWebhook_InvalidSignature`, (4) `TestHandleSubscriptionWebhook_UnknownEventType`. For push_test.go: (1) `TestSendPushNotification_ValidPayload`, (2) `TestSendPushNotification_MissingToken`, (3) `TestSendPushNotification_RespectsUserPreferences`, (4) `TestSendVoIPPush_FormatsAPNsPayload`.

---

### PILLAR B: Mobile Service + Hook + Component Tests (7 tasks)

Gap analysis found 10 untested services, 1 untested hook, and 34 untested components. These tasks target all remaining untested services and the highest-value components.

**Task 6: Service tests — creator + data** — Create `mobile/src/__tests__/services/creatorDashboard.test.ts`, `dataExport.test.ts`. For creatorDashboard: test fetch dashboard stats, test earnings summary, test content listing, test error handling on API failure. For dataExport: test request data export triggers API, test download export file, test export status polling. Mock `api/client`.

**Task 7: Service tests — engagement** — Create `mobile/src/__tests__/services/appRating.test.ts`, `feedbackCollection.test.ts`. For appRating: test rating prompt conditions (session count, days since install), test rating submission, test cooldown after dismissal. For feedbackCollection: test submit feedback with reason, test feedback categories list, test screenshot attachment. Mock `react-native-rate` or equivalent and `api/client`.

**Task 8: Service tests — spatial + social** — Create `mobile/src/__tests__/services/mapView.test.ts`, `linkPreview.test.ts`. For mapView: test location permission request, test nearby users fetch, test map marker generation, test distance calculation. For linkPreview: test URL metadata extraction, test image preview loading, test fallback for invalid URLs, test cache behavior. Mock location APIs and `api/client`.

**Task 9: Service tests — profile + timeline** — Create `mobile/src/__tests__/services/profileBoost.test.ts`, `timeline.test.ts`, `quickActions.test.ts`. For profileBoost: test boost activation, test cooldown timer, test boost status check. For timeline: test feed fetch with pagination, test filter by type, test empty state. For quickActions: test action dispatch, test action list. Mock `api/client`.

**Task 10: Service test — device integration** — Create `mobile/src/__tests__/services/intiface.test.ts`. Test device discovery scan, test connection handshake, test disconnect cleanup, test vibration command dispatch, test battery level query, test error state on connection failure. Mock `buttplug.io` client library.

**Task 11: Hook test — accessibility** — Create `mobile/src/__tests__/hooks/useAccessibility.test.ts`. Test screen reader detection, test font scale preference, test reduced motion preference, test color contrast mode, test accessibility announcement dispatch. Mock `react-native` AccessibilityInfo API.

**Task 12: Component tests — high-value** — Create `mobile/src/__tests__/components/ProfileCard.test.tsx`, `MatchCard.test.tsx`, `ChatInput.test.tsx`, `SettingsToggle.test.tsx`. For ProfileCard: test renders user photo + name + age, test verified badge display, test online indicator. For MatchCard: test renders match info, test swipe gesture handlers, test super-like variant. For ChatInput: test text input, test send button enabled/disabled, test attachment picker trigger. For SettingsToggle: test toggle state, test onPress callback, test disabled state. These are the 4 most-reused components after ItemCard and MessageBubble (already covered in Batch 7).

---

### PILLAR C: Web Page + Lib + Component Tests (5 tasks)

Gap analysis found 20 untested pages, 7 untested lib files, and 24 untested components. These tasks cover the remaining high-priority routes and critical lib files.

**Task 13: Web page tests — admin routes** — Create tests for `web/src/app/admin/dashboard/page.test.tsx`, `web/src/app/admin/reports/page.test.tsx`, `web/src/app/admin/verification/page.test.tsx`. For dashboard: test renders stat cards, test loading state, test requires admin auth. For reports: test renders report list, test filter controls, test report detail expansion. For verification: test renders pending verifications, test approve/reject actions, test status badge rendering. Mock fetch and auth context.

**Task 14: Web page tests — auth + discovery** — Create tests for `web/src/app/login/page.test.tsx`, `web/src/app/register/page.test.tsx`, `web/src/app/discover/page.test.tsx`. For login: test renders email/password fields, test validation errors, test submit triggers auth API, test redirect on success. For register: test renders registration form, test age verification gate, test terms acceptance required. For discover: test renders profile cards, test swipe controls, test empty state when no profiles. Mock auth service and fetch.

**Task 15: Web page tests — user routes** — Create tests for `web/src/app/settings/page.test.tsx`, `web/src/app/settings/account/page.test.tsx`, `web/src/app/home/page.test.tsx`, `web/src/app/companion/page.test.tsx`. For settings: test renders setting sections, test navigation to sub-pages. For settings/account: test renders account info, test delete account flow. For home: test renders main feed, test navigation tabs. For companion: test renders companion interface, test connection state. Mock fetch and auth.

**Task 16: Web lib tests — networking** — Create `web/src/lib/__tests__/api.test.ts`, `web/src/lib/__tests__/chatSocket.test.ts`, `web/src/lib/__tests__/callSignaling.test.ts`. For api: test request with auth header, test error response handling, test retry logic, test base URL configuration. For chatSocket: test connection lifecycle, test message send/receive, test typing indicators, test reconnection on disconnect. For callSignaling: test offer/answer exchange, test ICE candidate relay, test call end signal. Mock WebSocket and fetch.

**Task 17: Web component tests** — Create tests for 4 high-value components: `web/src/components/__tests__/Navbar.test.tsx`, `ProfileCard.test.tsx`, `MessageList.test.tsx`, `PaymentForm.test.tsx`. For Navbar: test renders nav links, test active route highlighting, test mobile hamburger menu. For ProfileCard: test renders user info, test action buttons (like/pass). For MessageList: test renders messages, test scroll behavior, test empty state. For PaymentForm: test renders card input, test validation, test submit. Mock router and API.

---

### PILLAR D: VR EditMode Test Expansion (3 tasks)

Gap analysis found ~62 of 71 VR scripts are untested (9 test files exist covering booth, haptic, marketplace, inventory, and basic systems). These tasks cover the most critical untested systems.

**Task 18: VR EditMode tests — core systems** — Create `vr-dryft/Assets/Tests/EditMode/GameManagerTests.cs`, `SessionManagerTests.cs`. For GameManager: test initialization sets default state, test scene transition triggers cleanup, test game state persistence, test event subscription/unsubscription. For SessionManager: test session start creates valid session ID, test session end records duration, test session timeout handling, test reconnection restores state. Mock Unity lifecycle and network calls.

**Task 19: VR EditMode tests — auth + networking** — Create `vr-dryft/Assets/Tests/EditMode/AuthManagerTests.cs`, `TokenStorageTests.cs`, `DryftRealtimeTests.cs`. For AuthManager: test login stores token, test logout clears state, test token refresh before expiry, test invalid token handling. For TokenStorage: test save/load token from PlayerPrefs, test token expiry check, test clear all tokens. For DryftRealtime: test connection initialization, test message send format, test message receive parsing, test reconnect logic, test disconnect cleanup.

**Task 20: VR EditMode tests — tutorial + avatar** — Create `vr-dryft/Assets/Tests/EditMode/TutorialManagerTests.cs`, `AvatarControllerTests.cs`. For TutorialManager: test step progression, test skip tutorial flag, test completion callback, test reset tutorial state. For AvatarController: test avatar initialization with default appearance, test customization application (hair, outfit, etc.), test avatar state serialization/deserialization, test avatar visibility toggle.

---

**Validation for all tasks**: `cd backend && go build ./cmd/dryft-api` must succeed. `cd backend && go test ./... -count=1 -timeout=120s` must pass. `cd web && npm test` must pass (uses Vitest, NOT Jest). `cd mobile && npm test -- --watchAll=false` must pass. VR: Unity must compile with no `error CS` diagnostics. Do NOT break existing tests. Do NOT generate `pnpm-lock.yaml` — this project uses npm. Update AGENTS_COLLAB.md with completion summary when done.

- [2026-03-02] CLAUDE-Architect – Scope: WebSocket production debugging, Nginx proxy root-cause, DreamHost support ticket. **Context**: After Neon + /health went green, HUMAN-Grant asked CLAUDE to debug WebSocket failures through the DreamHost proxy. **SSH debugging session** on VPS (`vps40055.dreamhostps.com`) confirmed three cascading bugs in the Go binary: (1) auth context key type mismatch (`string "user"` vs typed `contextKey("user")`) caused 401 on all authenticated WS connections; (2) `metrics.responseWriter` wrapper didn't implement `http.Hijacker`, causing 500 (`response does not implement http.Hijacker`); (3) Chi's `middleware.Timeout` wraps ResponseWriter with `timeoutWriter` (no Hijacker), also causing 500. **Code fixes applied**: (a) `backend/internal/realtime/handler.go` — switched to `OptionalAuth` + manual token extraction from query param; (b) `backend/internal/metrics/metrics.go` — added `Hijack()` method delegating to underlying writer; (c) `backend/cmd/dryft-api/main.go` — added `skipForWebSocket()` wrapper that bypasses Timeout middleware for `Upgrade: websocket` requests. **Three-way WebSocket test results**: (1) `ws://api.dryft.site:8080/v1/ws` (direct to Go, bypasses Nginx) → **101 Switching Protocols ✅**; (2) `wss://api.dryft.site/v1/ws` (through Nginx proxy) → **400 Bad Request ❌**; (3) `ws://localhost:8080/v1/ws` (VPS loopback) → **101 ✅**. **Root cause of Nginx 400**: DreamHost's managed Nginx proxy strips `Upgrade` and `Connection` headers and downgrades HTTP/1.1 to HTTP/1.0 before forwarding to the Go binary. Logs confirmed: `Upgrade=""`, `Connection="close"`, `HTTP/1.0`. **Working production endpoint**: `ws://api.dryft.site:8080/v1/ws` — port 8080 is directly accessible from the internet (confirmed). Port 8443 also externally accessible (tested with Python socket listener) for future TLS-on-Go option. **DreamHost support ticket filed** (Mar 2 2026, 24hr turnaround) requesting: `proxy_http_version 1.1;`, `proxy_set_header Upgrade $http_upgrade;`, `proxy_set_header Connection "upgrade";`. Once applied, all clients can use `wss://api.dryft.site/v1/ws`. **Docs updated**: `CHANGELOG.md` (root + backend), `RUNBOOK.md`, `docs/TROUBLESHOOTING.md`, `infra/DREAMHOST_DEPLOYMENT.md`, `backend/WEBSOCKET_EVENTS.md`. **Commit**: e9421ed on `claude/determined-carson`.

- [2026-03-03] HUMAN-Grant + Perplexity – Scope: DreamCompute migration and client wiring. **Status**: API is fully live on DreamCompute (`dryft-api-1`, `https://api.dryft.site/health` = 200), Nginx terminates TLS and proxies to `dryft-api` on port 8080, and web/mobile clients have been updated to read their API base URL from env (`NEXT_PUBLIC_API_BASE_URL`, `EXPO_PUBLIC_API_URL`). **Current issue**: local web dev environment is still using `NEXT_PUBLIC_API_BASE_URL=http://localhost:8080` via `.env.local`, so login requests from `http://localhost:3000/login` go to `http://localhost:8080/v1/auth/login` and fail with `Failed to load resource: Could not connect to the server`, even though `curl https://api.dryft.site/health` returns 200 from the same machine. **Next action for CLAUDE-Architect**: (1) Normalize web env handling so dev/prod both consistently point at `https://api.dryft.site` when desired; (2) verify `web/src/lib/api.ts` correctly prefers `NEXT_PUBLIC_API_BASE_URL` / `NEXT_PUBLIC_API_URL`; (3) ensure there is no hardcoded `http://localhost:8080` fallback in the login flow or auth client that bypasses the shared API base URL. Once fixed, re-run the login flow and confirm Network requests hit `https://api.dryft.site/v1/auth/login` and appear in `journalctl -u dryft-api.service` on DreamCompute.

- [2026-03-03] CLAUDE-Architect – Scope: DreamCompute health check + WebSocket verification + Batch 5 review + Batch 6 assignment. **SSH session** on DreamCompute (`208.113.200.211`): found both `dryft-api.service` and `nginx.service` inactive/dead (stopped cleanly at 20:40:54 UTC, likely VM restart). Restarted both; health confirmed within 3 seconds (`/health` → 200). **WebSocket verification through DreamCompute Nginx**: `curl` with `Upgrade: websocket` headers to `https://api.dryft.site/v1/ws` returned **401 Unauthorized** (not 400) — this means Nginx correctly forwards WebSocket upgrade headers to the Go binary, which then rejects due to missing auth token. This is the expected behavior and confirms **Phase 6 WebSocket proxying is WORKING** on DreamCompute (contrast with old DreamHost VPS which returned 400 because it stripped Upgrade headers). **Batch 5 review**: all 20 tasks delivered across 51 files (+4,146/-1,034 lines). All 30 Go packages pass, all 50 web test files pass (141 tests). Security review PASS. Removed stray `pnpm-lock.yaml` from Codex. Committed as `802f325` and pushed. **Batch 6 assigned** below (20 tasks: backend thin test expansion + mobile coverage + docs).

- [2026-03-03] CLAUDE-Architect → CODEX-Refactor – **BATCH 6 ASSIGNMENT (20 tasks)** — Three pillars: (A) expand 5 thin backend service_unit_test.go files to production-grade coverage, (B) close the massive mobile test gap (27 untested hooks, 39 untested services), (C) backend operational documentation. After Batch 6, every backend service_unit_test.go should have ≥5 test functions, and mobile test coverage should double from 47 to ~70+ files.

---

### PILLAR A: Backend Thin Service Test Expansion (5 tasks)

Batches 4-5 created service_unit_test.go files for these packages, but they are dangerously thin (1-2 test functions each). Expand each to ≥6 meaningful test functions covering the actual service methods. Use the existing mock/interface patterns already in the file. Do NOT delete existing tests — ADD to them.

**Task 1: Expand admin service_unit_test.go (currently 2 tests → target ≥8)** — `internal/admin/service_unit_test.go`. Service methods: `GetVerifications`, `ApproveVerification`, `RejectVerification`, `GetPendingReports`, `ReviewReport`, `GetUser`, `BanUser`, `UnbanUser`, `GetDashboardStats`, `IsAdmin`. Add tests: (1) `TestGetVerifications_FiltersByStatus` — verify status param filters results, (2) `TestApproveVerification_RequiresAdminRole` — non-admin caller rejected, (3) `TestRejectVerification_RequiresReason` — empty reason string rejected, (4) `TestBanUser_SetsCorrectFlags` — verify ban flag and audit trail, (5) `TestUnbanUser_RestoresFlags` — verify flags cleared, (6) `TestGetDashboardStats_AggregatesCorrectly` — verify stat fields populated. Mock the `*database.DB` queries as done in existing tests.

**Task 2: Expand analytics service_unit_test.go (currently 2 tests → target ≥8)** — `internal/analytics/service_unit_test.go`. Service methods: `IngestBatch`, `GetUserAnalytics`, `GetDailyMetrics`, `GetEventCounts`, `GetRecentEvents`, `GetTopEvents`, `AggregateDailyMetrics`, `Stop`. Add tests: (1) `TestIngestBatch_ValidatesRequiredFields` — batch with missing event_type rejected, (2) `TestIngestBatch_RespectsMaxBatchSize` — oversized batch rejected, (3) `TestGetUserAnalytics_ReturnsEmptyForNewUser` — no error for unknown user, (4) `TestGetDailyMetrics_ValidatesDateRange` — from > to rejected, (5) `TestGetEventCounts_GroupsByType` — verify grouping logic, (6) `TestGetRecentEvents_RespectsLimit` — verify limit param. Note: analytics uses `*gorm.DB` not `*database.DB`; use `sqlmock` or test against the struct's public interface.

**Task 3: Expand calls service_unit_test.go (currently 1 test → target ≥6)** — `internal/calls/service_unit_test.go`. The calls package has `SignalingHub` (signaling.go, 15 methods) and `Handler` (handler.go). Focus on SignalingHub unit logic: (1) `TestNewSignalingHub_InitializesEmptyState`, (2) `TestRegisterConnection_AddsUserToHub` — verify `IsUserOnline` returns true after register, (3) `TestUnregisterConnection_RemovesUser` — verify `IsUserOnline` returns false after unregister, (4) `TestIsUserInCall_FalseWhenNotInCall`, (5) `TestGetActiveCall_ReturnsNilForUnknown`, (6) `TestGetUserActiveCall_ReturnsNilWhenIdle`. Use a mock `CallRepository` interface (check if one exists, or define one from the handler constructor signature).

**Task 4: Expand chat service_unit_test.go (currently 2 tests → target ≥7)** — `internal/chat/service_unit_test.go`. Service methods: `GetConversations`, `GetConversation`, `GetMessages`, `SendMessage`, `MarkAsRead`, `GetConversationByMatch`. Add tests: (1) `TestSendMessage_RejectsEmptyConversationID` — zero UUID rejected, (2) `TestSendMessage_ValidatesMessageType` — invalid type rejected, (3) `TestGetConversations_RespectsLimit` — verify limit/offset params, (4) `TestMarkAsRead_RequiresValidUser` — zero user ID rejected, (5) `TestGetConversationByMatch_ReturnsNilForNoMatch`. Mock `ChatNotifier` interface and `*database.DB`.

**Task 5: Expand matching service_unit_test.go (currently 1 test → target ≥7)** — `internal/matching/service_unit_test.go`. Service methods: `Swipe`, `GetMatches`, `Unmatch`, `GetDiscoverProfiles`, `GetMatchedUserIDs`, `GetPresenceRecipients`, `GetMatch`. Add tests: (1) `TestSwipe_RejectsInvalidDirection` — direction not "like"/"pass" rejected, (2) `TestSwipe_RejectsSameUser` — already tested, keep it, (3) `TestSwipe_RejectsZeroUUID` — zero swiper/swiped ID rejected, (4) `TestGetMatches_RespectsLimit` — verify limit param, (5) `TestUnmatch_RejectsZeroMatchID`, (6) `TestGetDiscoverProfiles_RespectsLimit`. Mock `MatchNotifier` interface and `*database.DB`.

---

### PILLAR B: Mobile Test Coverage (10 tasks)

Mobile has 32 hooks and 44 services but only 5 hooks and 5 services are tested (47 total test files). These tasks target the highest-value untested files. Use React Native Testing Library + Jest mocks. Follow the patterns in existing tested hooks/services (e.g., `mobile/src/__tests__/hooks/useCalls.test.ts`, `mobile/src/__tests__/services/chatSocket.test.ts`). Each test file should have ≥3 test cases.

**Task 6: Mobile hook tests — security** — Create `mobile/src/__tests__/hooks/useScreenSecurity.test.ts` (271 lines), `useLivenessDetection.test.ts`, `useModeration.test.ts`. For useScreenSecurity: test enable/disable screen capture protection, screenshot detection callback, background blur activation. For useLivenessDetection: test challenge initiation, face detection callback, timeout handling, success/failure states. For useModeration: test content flag submission, report status tracking, appeal flow initiation. Mock native modules (`react-native` ScreenCapture, Camera, etc.) with `jest.mock`.

**Task 7: Mobile hook tests — communication** — Create `mobile/src/__tests__/hooks/useVoiceChat.test.ts` (325 lines), `useVoiceMessage.test.ts`, `useCompanionSession.test.ts`. For useVoiceChat: test start/stop call, mute/unmute toggle, speaker toggle, connection state transitions (idle→connecting→connected→ended). For useVoiceMessage: test start/stop recording, playback control, duration tracking, send callback. For useCompanionSession: test session creation, device pairing, session state sync, disconnect cleanup. Mock WebRTC and audio APIs.

**Task 8: Mobile hook tests — data & engagement** — Create `mobile/src/__tests__/hooks/useAnalytics.test.ts` (360 lines), `useEngagementMetrics.test.ts`, `useFavorites.test.ts`, `useTimeline.test.ts`. For useAnalytics: test event tracking, screen view logging, user properties, flush on unmount. For useEngagementMetrics: test session duration tracking, interaction counting, metric submission. For useFavorites: test add/remove favorite, list favorites, optimistic update. For useTimeline: test fetch timeline items, pagination, pull-to-refresh, item ordering.

**Task 9: Mobile hook tests — utility** — Create `mobile/src/__tests__/hooks/useDebounce.test.ts` (22 lines — small, quick test), `useInterval.test.ts`, `usePrevious.test.ts`, `useThrottle.test.ts`, `useMounted.test.ts`, `useOffline.test.ts`. These are small utility hooks. For useDebounce: test value updates after delay, reset on new value. For useInterval: test callback fires at interval, clears on unmount. For usePrevious: test returns previous value. For useThrottle: test rate-limits calls. For useMounted: test returns false during unmount. For useOffline: test online/offline state detection. Use `jest.useFakeTimers()` for timer-dependent hooks.

**Task 10: Mobile hook tests — features** — Create `mobile/src/__tests__/hooks/useHaptic.test.ts` (332 lines), `useI18n.test.ts`, `usePurchases.test.ts`, `useRewards.test.ts`. For useHaptic: test device registration, pattern send, connection state, disconnect cleanup. For useI18n: test locale detection, translation lookup, language switch, fallback locale. For usePurchases: test product listing, purchase initiation, receipt validation, restore purchases. For useRewards: test reward balance fetch, claim reward, reward history. Mock `expo-haptics`, `expo-localization`, `react-native-purchases`.

**Task 11: Mobile service tests — core** — Create `mobile/src/__tests__/services/api.test.ts` (5 lines — tiny wrapper, test config), `analytics.test.ts` (1154 lines — the largest service), `notifications.test.ts` (1046 lines). For api: test base URL configuration, auth header injection, error response parsing. For analytics: test event batching, flush interval, session tracking, user identification, opt-out toggle, event schema validation. For notifications: test permission request, token registration, notification display, deep link handling from notification tap, badge count management. Mock `@react-native-firebase/messaging`, `expo-notifications`.

**Task 12: Mobile service tests — calls** — Create `mobile/src/__tests__/services/callSignaling.test.ts` (249 lines), `voiceMessage.test.ts` (733 lines), `voipPush.test.ts`, `webrtc.test.ts` (277 lines). For callSignaling: test signal message send/receive, connection lifecycle, ICE candidate handling, call state machine. For voiceMessage: test recording start/stop, audio file management, upload progress, playback controls, duration validation. For voipPush: test VoIP push registration, incoming call handling, call kit integration. For webrtc: test peer connection setup, media stream acquisition, track management, connection state. Mock `react-native-webrtc`, `react-native-callkeep`.

**Task 13: Mobile service tests — security** — Create `mobile/src/__tests__/services/twoFactorAuth.test.ts` (554 lines), `screenSecurity.test.ts`, `livenessDetection.test.ts` (753 lines), `safety.test.ts` (593 lines). For twoFactorAuth: test TOTP setup flow, code verification, backup codes generation, 2FA enable/disable toggle, recovery flow. For screenSecurity: test screenshot prevention enable/disable, screen recording detection. For livenessDetection: test face detection pipeline, challenge-response flow, anti-spoofing checks, timeout handling. For safety: test SOS alert trigger, emergency contact notification, location sharing, block/report user, safety score calculation. Mock camera/biometric native modules.

**Task 14: Mobile service tests — social** — Create `mobile/src/__tests__/services/couples.test.ts`, `gifts.test.ts` (581 lines), `favorites.test.ts`, `icebreakers.test.ts` (576 lines). For couples: test partner invitation, acceptance flow, shared profile, breakup cleanup. For gifts: test gift catalog fetch, gift purchase, gift send with message, gift receive notification, gift history. For favorites: test add/remove, list fetch, sort order, max favorites limit. For icebreakers: test icebreaker prompt generation, prompt selection, response submission, match icebreaker reveal, daily icebreaker refresh.

**Task 15: Mobile service tests — platform** — Create `mobile/src/__tests__/services/deepLinking.test.ts` (981 lines), `backgroundTasks.test.ts`, `cacheManager.test.ts`, `offlineSupport.test.ts`. For deepLinking: test URL parsing, route resolution, deferred deep link handling, universal link configuration, scheme handling. For backgroundTasks: test task registration, task execution on schedule, minimum interval enforcement, task completion reporting. For cacheManager: test cache write/read, TTL expiration, cache invalidation, max size eviction. For offlineSupport: test offline queue, sync on reconnect, conflict resolution, offline indicator state. Mock `expo-linking`, `expo-task-manager`, `@react-native-async-storage/async-storage`.

---

### PILLAR C: Backend Documentation (5 tasks)

**Task 16: Mobile service tests — media & remaining** — Create `mobile/src/__tests__/services/chatMedia.test.ts`, `uploads.test.ts`, `stories.test.ts`, `appUpdate.test.ts`. For chatMedia: test image/video selection, compression, upload with progress, thumbnail generation. For uploads: test file validation (type, size), multipart upload, retry on failure, upload cancellation. For stories: test story creation, story feed fetch, story expiration (24hr), story view tracking. For appUpdate: test version check, update prompt display, force update enforcement, skip update for optional versions. Mock `expo-image-picker`, `expo-file-system`.

**Task 17: Backend RUNBOOK.md** — Create `backend/RUNBOOK.md` covering operational procedures. Sections: (1) **Service Start/Stop/Restart** — systemd commands for `dryft-api.service`, log viewing with `journalctl -u dryft-api -f`; (2) **Health Check** — `curl https://api.dryft.site/health` expected response, what each field means; (3) **Database** — Neon dashboard URL, connection string format, running manual migrations; (4) **Deployment** — build binary (`go build ./cmd/dryft-api`), upload to `/opt/dryft/`, restart service, rollback procedure (swap with `.prev` binary); (5) **Monitoring** — key log patterns to watch (ERROR, WARN, panic), Prometheus metrics endpoint; (6) **Secrets Rotation** — how to rotate JWT_SECRET_KEY, ENCRYPTION_KEY without downtime; (7) **Common Operations** — clear rate limit state (restart service), check active WebSocket connections, verify TLS cert expiry. Keep it concise and actionable — commands over prose.

**Task 18: Backend TROUBLESHOOTING.md** — Create `backend/TROUBLESHOOTING.md`. Sections organized as Problem → Cause → Fix: (1) **"health returns unhealthy"** — check Neon connection, DNS resolution, .env.prod DATABASE_URL; (2) **"401 on all requests"** — JWT_SECRET_KEY mismatch between instances, token expired, missing Authorization header; (3) **"WebSocket 400 Bad Request"** — Nginx not forwarding Upgrade headers (check `proxy_http_version 1.1`, `proxy_set_header Upgrade`); (4) **"WebSocket 401 Unauthorized"** — missing `?token=` query param on WS connect URL; (5) **"Redis connection failed"** — expected if REDIS_URL is empty (in-memory fallback), not an error; (6) **"migration failed"** — check migration file syntax, verify database user has CREATE/ALTER permissions on Neon; (7) **"binary won't start"** — check GOOS/GOARCH match target (linux/amd64 for DreamCompute), check port 8080 not already in use; (8) **"TLS cert expired"** — run `certbot renew`, reload nginx; (9) **"high memory usage"** — check active WebSocket connection count, possible goroutine leak in realtime package. Reference RUNBOOK.md for fix procedures.

**Task 19: Backend request ID propagation** — Currently `backend/internal/middleware/requestid.go` generates request IDs but they aren't propagated through the structured logger. Modify the request ID middleware to inject the request ID into the context-bound `slog` logger so every log line within a request includes `"request_id": "..."`. Implementation: in `requestid.go` middleware, after setting the request ID in context, create a child logger with `slog.With("request_id", requestID)` and store it in the context. Then update `backend/internal/logger/logger.go` to provide a `FromContext(ctx)` function that retrieves the logger from context. Add tests in `middleware/requestid_test.go` to verify the request ID appears in log output. This enables request tracing across log lines.

**Task 20: Expand mobile hook tests — remaining hooks** — Create `mobile/src/__tests__/hooks/useAppUpdate.test.ts`, `useCouples.test.ts`, `useCreatorDashboard.test.ts`, `useQuickActions.test.ts`, `useNotificationActions.test.ts`, `useNotificationCenter.test.ts`. For useAppUpdate: test update check trigger, update available state, install prompt. For useCouples: test partner status, invite flow, shared state sync. For useCreatorDashboard: test earnings fetch, analytics display, content management. For useQuickActions: test action registration, action execution, badge updates. For useNotificationActions: test action button handling, mark-as-read, dismiss. For useNotificationCenter: test notification list, unread count, clear all.

---

**Validation for all tasks**: `cd backend && go build ./cmd/dryft-api` must succeed. `cd backend && go test ./... -count=1 -timeout=120s` must pass (skip integration tests). `cd web && npm test` must pass (uses Vitest, NOT Jest — do NOT use `--watchAll=false` flag). `cd mobile && npm test -- --watchAll=false` must pass. Do NOT break existing tests. Do NOT generate `pnpm-lock.yaml` — this project uses npm. Update AGENTS_COLLAB.md with completion summary when done.

- [2026-03-03] CODEX-Refactor – Scope: `backend/internal/{admin,analytics,calls,chat,matching,middleware,logger}`, `backend/{RUNBOOK.md,TROUBLESHOOTING.md}`, `mobile/src/__tests__/{hooks,services}/**`, `mobile/src/__tests__/setup.ts`, `mobile/src/utils/accessibility/index.ts`, `mobile/package.json` – Completed **Batch 6 (20 tasks)** across all three pillars: (1) expanded thin backend service unit tests for `admin`, `analytics`, `calls`, `chat`, and `matching` to production-grade depth by adding validation-focused coverage and extending test counts to requested targets; (2) added request-ID logger propagation by wiring `middleware.RequestID` to inject a request-scoped `slog` logger into context, added `logger.WithContext` / `logger.FromContext`, and added `request_id_test.go` assertions that log output includes `"request_id"`; (3) added backend operational docs `backend/RUNBOOK.md` and `backend/TROUBLESHOOTING.md` with DreamCompute/systemd/neon/nginx workflows and problem→cause→fix playbooks; (4) executed the full mobile coverage expansion with new hook/service suites for security, communication, data/engagement, utility, feature, social, platform, calls, and media/update domains, including the “remaining hooks” set (`useAppUpdate`, `useCouples`, `useCreatorDashboard`, `useQuickActions`, `useNotificationActions`, `useNotificationCenter`); (5) stabilized mobile test harness for native/Expo modules with explicit Jest mocks, resolved the accessibility/theme initialization crash by removing the `ThemeProvider` import cycle in `accessibility/index.ts`, and fixed RevenueCat Jest ESM parsing by extending `transformIgnorePatterns` to include `react-native-purchases`/`@revenuecat/*`; (6) ensured CI/test-command compatibility note is honored: web validation uses `npm test` (Vitest, no Jest `--watchAll=false`), and no `pnpm-lock.yaml` was generated. Validation: `cd backend && go build ./cmd/dryft-api` ✅, `cd backend && go test ./... -count=1 -timeout=120s` ✅, `cd web && npm test` ✅ (`50` files / `141` tests), `cd mobile && npm test -- --watchAll=false` ✅ (`96` suites / `274` tests).

- [2026-03-03] CODEX-VR – Scope: `vr-dryft` Unity recovery + Normcore install + baseline scene scaffolding. **Work completed**: (1) cleared blocker compile errors enough to get out of Safe Mode and reconnect UnityMCP; (2) installed Normcore via UPM by adding `com.normalvr.normcore@3.2.2` to `Packages/manifest.json` and ensuring Normcore scoped registry resolution (`com.normalvr` scope on `https://normcore-registry.normcore.io`); lockfile now includes the package; (3) updated setup validation to detect installed Normcore correctly in `Assets/Editor/SetupGuide.cs` (`com.normalvr.normcore` check); (4) added editor automation `Assets/Editor/DefaultVRSceneGenerator.cs` and generated baseline scenes/build-order: `Assets/Scenes/{Bootstrap,Login,Verification,Lobby,Bar_Main,Booth_Private}.unity`; (5) generated environment placeholder materials under `Assets/Materials/Environment` and screenshots under `Assets/Screenshots`; (6) build settings now include the six generated scenes in order. **Notes**: current generated environment is an initial graybox scaffold for collaborative layout iteration; Unity was restarted during package/refresh operations and MCP reconnect is expected after each domain reload.

- [2026-03-04] CODEX-VR – Scope: `vr-dryft` Safe Mode recovery after Normcore install. **Work completed**: (1) identified root cause of regenerated compile failures as local compatibility files shadowing real Normcore package types and generated model code; (2) removed `Assets/Scripts/Compatibility/NormcoreStubs.cs` and `Assets/Scripts/Compatibility/NormcoreModelFallbacks.cs` (and their `.meta` files), eliminating the `CS0436`/`CS0115`/`CS0102` conflict chain from source generators; (3) updated `Assets/Scripts/Dryft.asmdef` to reference both `Normal.Realtime` and `Normal.Realtime.Shared` so Normcore core + avatar/voice types resolve from installed package assemblies rather than deleted stubs; (4) fixed remaining hard compile errors in `Assets/Scripts/Networking/PlayerSyncModel.cs` by enabling notify events on `_status` and `_isMuted` (`[RealtimeProperty(..., true, true)]`) so `PlayerSync` subscriptions to `statusDidChange` / `isMutedDidChange` compile against generated model code; (5) verified with Unity Roslyn invocation (`csc` using `Library/Bee/artifacts/.../Drift.rsp`) that `Drift` now compiles with warnings only and no errors. **Next step in Editor**: let Unity finish reimport/recompile, then exit Safe Mode once no red compile errors remain.

- [2026-03-04] CODEX-VR – Scope: final project setup completion in running Unity editor. **Work completed**: (1) validated setup prerequisites in-editor (Normcore/XR packages and scene set present); (2) enabled Android XR loader configuration for Oculus in `Assets/XR/XRGeneralSettingsPerBuildTarget.asset` (`Android Providers` now auto-loads Oculus loader); (3) enforced Quest Android player settings in `ProjectSettings/ProjectSettings.asset` (IL2CPP backend, `NORMCORE` define on Android, ARM64 architecture, min/target SDK already aligned); (4) added `Assets/Editor/SetupAutomation.cs` with menu action `Drift/Setup/Complete Project Setup` and executed it successfully (`[DriftSetup] CompleteProjectSetup finished.` in console); (5) confirmed Build Settings scene order is set for `Bootstrap -> Login -> Verification -> Lobby -> Bar_Main -> Booth_Private`; (6) executed Android dependency resolution command via `Assets/Mobile Dependency Resolver/Android Resolver/Resolve`; (7) final console check shows no C# compile errors.

- [2026-03-04] CODEX-VR – Scope: environment gameplay wiring pass for generated VR scenes. **Work completed**: (1) added `Assets/Editor/EnvironmentGameplayPass.cs` with menu action `Drift/Setup/Apply Environment Gameplay Pass`; (2) implemented idempotent scene wiring for `Bar_Main` and `Booth_Private` to ensure required gameplay objects/components are present and linked (`AudioManager`, `Jukebox`, `Mirror`, `BoothPortal`, `Seat`, `ZoneManager` zones, `HapticZone`); (3) generated/assigned required helper children and assets (sit/look points, reflection cameras, portal VFX hooks, zone colliders/spawn points, mirror render texture resource) without destructive scene replacement; (4) executed the pass successfully in Unity (`[EnvironmentGameplayPass] Completed scene gameplay wiring pass.`); (5) re-ran Unity refresh/compile checks after setup and confirmed no C# compile errors in console filters (`error CS`, `): error` both zero).

- [2026-03-04] CODEX-VR – Scope: first Bar_Main art + lighting polish pass. **Work completed**: (1) added `Assets/Editor/BarMainArtLightingPass.cs` with menu action `Drift/Setup/Apply Bar_Main Art + Lighting Pass` for repeatable non-destructive visual iteration; (2) defined/retuned environment materials to a cohesive neon-night palette (core surfaces, neon signs, booth portals, plus new utility materials `Bar_Wall`, `Bar_Trim`, `Bar_Glow_Cyan`, `Bar_Glow_Magenta`); (3) applied render settings pass (tri-light ambient + exponential-squared fog) and rebuilt key lighting rig (cool low directional fill, stronger neon point lights, dancefloor spotlights, bar rim lights); (4) added set-dressing geometry under `Art_SetDressing` (back/side walls, dancefloor trim, under-bar glow strips, ceiling rails) and hid the generated scene label for cleaner presentation; (5) tuned `AtmosphericFog` particle values for larger low-speed volumetric haze; (6) executed the pass successfully (`[BarMainArtLightingPass] Applied first art + lighting pass to Bar_Main.`), captured comparison screenshot `Assets/Screenshots/Bar_Main_after_artpass_v1.png`, and confirmed no C# compile errors in console filters (`error CS`, `): error` both zero).

- [2026-03-05] CODEX-VR – Scope: disco dance floor feature pass for `Bar_Main` (70s checker + music-reactive strobe + disco ball). **Work completed**: (1) added runtime controller `Assets/Scripts/Environment/DiscoDanceFloorController.cs` (`[ExecuteAlways]`) to drive multi-rhythm checker strobe patterns, palette cycling, beat-flash response via `AudioManager.OnBeatDetected`, and disco-ball/sweep-light motion; (2) expanded `Assets/Editor/BarMainArtLightingPass.cs` to procedurally create and wire an `8x8` `DiscoTiles` grid over `DanceFloor`, generate `DiscoBallRig` (ball mesh + cyan/magenta/purple sweep spotlights + core point light), and assign serialized controller references/idempotent defaults; (3) added/tuned new materials for disco elements (`Bar_DiscoTile_Base.mat`, `Bar_DiscoBall.mat`) and included them in the art-pass material pipeline; (4) executed `Drift/Setup/Apply Bar_Main Art + Lighting Pass` after compile refresh, verified controller attachment and `64` tile renderer refs, and captured updated scene output `Assets/Screenshots/Bar_Main_disco_editmode_v2.png`; (5) post-pass console checks show no C# compile errors (`error CS` and `): error` both zero).

- [2026-03-05] CODEX-VR – Scope: Unity lockup/crash triage during disco pass iteration. **Findings + mitigation**: (1) MCP became unresponsive and Unity console calls timed out; (2) `~/Library/Logs/Unity/Editor.log` showed repeated `FileNotFoundException: Could not find file \"\"` from `UnityEditor.LevelPlay.LevelPlayInstaller.PackageIsInstalled()` and then a native `segv` crash, indicating editor instability not tied to C# compile errors; (3) project does not currently use Ads APIs in `Assets/` scripts, so removed `com.unity.ads` from `vr-dryft/Packages/manifest.json` to disable the failing LevelPlay editor integration path; (4) next step required in editor: restart Unity so UPM re-resolves packages without Ads and verify console stability before resuming scene iteration.

- [2026-03-05] CODEX-VR – Scope: pink/magenta environment regression fix after disco pass updates. **Root cause + fix**: (1) project currently runs on built-in render pipeline (`ProjectSettings/GraphicsSettings.asset` has `m_CustomRenderPipeline: {fileID: 0}`), so URP/Lit materials render as magenta fallback; (2) implemented pipeline-aware shader selection in `Assets/Editor/BarMainArtLightingPass.cs` and `Assets/Editor/DefaultVRSceneGenerator.cs` so material creation/conversion now prefers `Standard` when SRP is inactive and only uses `Universal Render Pipeline/Lit` when SRP is actually enabled; (3) corrected disco tile generation parenting bug by moving tiles to world-scale root `DiscoTilesRoot` (avoids inheriting `DanceFloor` scale and flooding the scene with oversized emissive tiles); (4) reverted active environment materials under `Assets/Materials/Environment/*.mat` back to `Standard` shader (except `_ShaderProbe_URP.mat`), refreshed/recompiled, and re-ran `Drift/Setup/Apply Bar_Main Art + Lighting Pass`; (5) verified result via screenshot `Assets/Screenshots/Bar_Main_after_pipeline_fix.png` and console checks showing no C# compile errors (`error CS` and `): error` both zero).

- [2026-03-05] CODEX-VR – Scope: Bar scene furnishing pass (tables/stools) + jukebox selection controls. **Work completed**: (1) extended `Assets/Editor/BarMainArtLightingPass.cs` to generate an idempotent `Furniture_Layout` with four lounge table clusters and stools (`TableCluster_01..04`) and to build a `JukeboxControlPanel` attached to `Jukebox_Interactable`; (2) added new materials for furniture and control buttons (`Bar_TableTop`, `Bar_TableBase`, `Bar_StoolSeat`, `Bar_StoolLeg`, `Jukebox_Button_{Prev,Play,Next}`, `Jukebox_Display`) and assigned them in pass; (3) added runtime control script `Assets/Scripts/Environment/Interactables/JukeboxControlButton.cs` (Previous / PlayPause / Next actions) extending `InteractableObject`; (4) upgraded `Assets/Scripts/Environment/Interactables/Jukebox.cs` to support world-space track display (`_worldTrackText`) and deterministic track selection via next/previous button presses; (5) upgraded `Assets/Scripts/Environment/AudioManager.cs` with explicit selection APIs (`PlayTrackAt`, `GetTrackCount`, `GetTrackNameAt`, `GetCurrentTrackIndex`) and auto-load fallback from `Resources/Audio/Tracks` when `_lofiTracks` is empty; (6) executed `Drift/Setup/Apply Bar_Main Art + Lighting Pass`, verified generated objects/components (`Furniture_Layout`, `JukeboxControlPanel`, 3 `JukeboxControlButton` components) and captured scene snapshot `Assets/Screenshots/Bar_Main_furnished_jukebox_v1.png`; (7) post-change compile checks are clean (`error CS` and `): error` both zero).

- [2026-03-05] CODEX-VR – Scope: cozy bar polish + AI-bartender-ready service station in `Bar_Main`. **Work completed**: (1) extended `Assets/Editor/BarMainArtLightingPass.cs` with `ApplyCozyBarProps()` and `ApplyBartenderStation()` to add backbar shelves, bottle rows, menu board, corner lamps, plants, and a dedicated bartender station cluster; (2) added bartender integration hook `Assets/Scripts/Environment/BartenderServicePoint.cs` and serialized station references (`BartenderAnchor`, `BartenderLookTarget`, `GuestSpot_A/B/C`, `BartenderServiceZone`) plus initial persona prompt (`bar_host_v1`) for future AI dialog/runtime logic; (3) increased social seating density by raising lounge table clusters to six and adding `CounterStools` along the bar, while keeping generation idempotent for repeat passes; (4) converted the bartender service volume to hidden trigger-only behavior (collider active, renderer disabled) so interaction logic remains available without visible debug geometry; (5) refreshed/compiled Unity, executed `Drift/Setup/Apply Bar_Main Art + Lighting Pass`, verified key objects (`Bar_CozyProps`, `BackbarShelves`, `Bartender_Station`, `CounterStools`) and `BartenderServicePoint` reference wiring, and captured updated screenshot `Assets/Screenshots/Bar_Main_cozy_bartender_v2.png`; (6) final console compile filters remain clean (`error CS` and `): error` both zero).

- [2026-03-05] CODEX-VR – Scope: bartender NPC + AI interaction path + jukebox bartender-request flow. **Work completed**: (1) added runtime bartender animation controller `Assets/Scripts/Environment/BartenderNPCController.cs` with procedural idle motion, look-target tracking, and a triggerable serve gesture loop; (2) upgraded `Assets/Scripts/Environment/BartenderServicePoint.cs` from data-only hook to full interactive service system (`InteractableObject`), including greeting flow, `SubmitVoiceTranscript(...)` entry point for STT/voice pipelines, local AI fallback reply logic by mood intent, optional remote AI endpoint hook (`/v1/vr/bartender/respond`), HUD feedback, and song recommendation/queue support; (3) extended jukebox integration by adding `OnRequestFromBartenderPressed()` + external display refresh in `Assets/Scripts/Environment/Interactables/Jukebox.cs`; (4) extended `Assets/Scripts/Environment/Interactables/JukeboxControlButton.cs` with new `RequestBartender` action; (5) expanded `Assets/Editor/BarMainArtLightingPass.cs` to auto-generate and wire a bartender NPC rig (`Bartender_NPC`), speech text mesh, updated station serialized refs, and a new world button `RequestBartenderButton` on the jukebox panel; (6) added/assigned new materials (`Jukebox_Button_Request`, `Bar_BartenderSkin`, `Bar_BartenderShirt`, `Bar_BartenderVest`) and prefab persistence output at `Assets/Prefabs/Environment/BartenderNPC.prefab`; (7) ran Unity refresh + re-applied `Drift/Setup/Apply Bar_Main Art + Lighting Pass`, confirmed key objects/components exist and serialized correctly (`BartenderServicePoint` -> NPC/speech refs, `JukeboxControlButton._action = RequestBartender`), captured updated screenshot `Assets/Screenshots/Bar_Main_bartender_ai_jukebox_v1.png`, and compile filters stayed clean (`error CS` and `): error` both zero).

- [2026-03-05] CODEX-VR – Scope: provider-editable LLM configuration for AI bartender (no hard API lock-in). **Work completed**: (1) added configurable ScriptableObject `Assets/Scripts/Environment/BartenderAISettings.cs` with provider switch (`OpenAICompatible`, `Anthropic`, `CustomJsonEndpoint`), endpoint/model/temperature/max-tokens settings, auth options (PlayerPrefs API key or inline), optional custom header, and local-fallback behavior control; (2) added transport/parser layer `Assets/Scripts/Environment/BartenderAIClient.cs` using `UnityWebRequest` with provider-specific request/response handling and custom JSON field extraction for `reply`/`recommended_track_index`; (3) refactored `Assets/Scripts/Environment/BartenderServicePoint.cs` to consume the settings asset (`_aiSettings`) and route prompts through `BartenderAIClient` while retaining robust local fallback when remote AI is disabled/unavailable; (4) updated `Assets/Editor/BarMainArtLightingPass.cs` to create/ensure and auto-assign `Assets/Resources/Config/BartenderAISettings.asset` into `BartenderServicePoint` during scene pass runs; (5) validated in running Unity after restart: pass executed successfully, compile filters remain clean (`error CS` and `): error` both zero), `BartenderServicePoint._aiSettings` points to `Assets/Resources/Config/BartenderAISettings.asset`, and captured screenshot `Assets/Screenshots/Bar_Main_bartender_llm_configurable_v1.png`.

- [2026-03-05] CODEX-VR – Scope: `Booth_Private` private-party environment expansion + invitation-only booth access hardening. **Work completed**: (1) added new invitation-safe interaction scripts `Assets/Scripts/Environment/Interactables/BoothAmbianceControlButton.cs` (world ambiance selectors) and `Assets/Scripts/Environment/Interactables/PrivatePartySpacePad.cs` (teleport pads for themed sub-spaces, with active-booth requirement and invite-only feedback); (2) added a new repeatable booth design pass `Assets/Editor/BoothPrivatePartyPass.cs` with menu action `Drift/Setup/Apply Booth_Private Party Pass`, generating cohesive private-party layout zones inside `Booth_Private` (architecture trim, couples lounge, group lounge, mini dance nook, ambiance control wall, and space-selection pads) plus booth-themed materials under `Assets/Materials/Environment` (`Booth_Trim`, `Booth_CouchCouple`, `Booth_CouchGroup`, `Booth_TableGlass`, `Booth_DanceTile`, `Booth_Pad*`, `Booth_AmbienceButton`); (3) updated `Assets/Scripts/Environment/Interactables/BoothPortal.cs` to enforce invitation basis at interaction time: inviters now receive “waiting for acceptance” feedback instead of solo-entering a booth, non-invited users are blocked, and direct entry is denied when `_requiresInvite` is true and no confirmed invite exists; (4) executed the new booth pass multiple times (idempotent behavior verified), confirmed generated party-space objects/components exist (`BoothParty_CouplesLounge`, `BoothParty_GroupLounge`, `BoothParty_DanceNook`, `AmbianceBtn_Romantic`, `SpacePad_Couples`) and captured updated scene output `Assets/Screenshots/Booth_Private_party_spaces_v2.png`; (5) post-change compile checks remain clean (`error CS` and `): error` both zero).

- [2026-03-05] CODEX-VR – Scope: invite-only private booth completion + host-governance/network signaling hardening. **Work completed**: (1) added booth invite signaling service `Assets/Scripts/Environment/BoothInviteService.cs` with websocket message handlers for `booth_invite`, `booth_invite_response`, `booth_privacy_update`, and `booth_host_control`; (2) extended `Assets/Scripts/Environment/BoothManager.cs` with host-governance state (`_inviteOnly`, `_roomLocked`, `_companionVoiceAllowed`, `_maxGuestCount`), privacy-change event, remote privacy sync handling, and host-control broadcast/receive support including `end_party`; (3) upgraded `Assets/Scripts/Environment/Interactables/BoothPortal.cs` to network-backed invite flow (send invite, receive invite, accept/decline response, inviter auto-enter on acceptance) while preserving strict invite-only gate behavior; (4) added `Assets/Scripts/Environment/Interactables/BoothHostControlButton.cs` and wired host actions (lock room, toggle invite-only, toggle companion voice, end party); (5) extended `Assets/Editor/BoothPrivatePartyPass.cs` to generate host-control panel/buttons and wire `BoothManager` refs, then removed deprecated editor API usage (`FindObjectOfType` → `FindFirstObjectByType`) so the pass runs without file-specific deprecation warnings; (6) executed `Drift/Setup/Apply Booth_Private Party Pass` successfully after final changes, verified generated objects/components in `Booth_Private` (`HostBtn_*`, `SpacePad_*`, `BoothManager` privacy fields), and verified `Bar_Main` booth portals have `_requiresInvite: true`; (7) final console compile checks are clean (`error CS` and `): error` both zero).
- [2026-03-05] CODEX-VR – Scope: in-world invite picker for private booths (nearby user selection). **Work completed**: (1) added synced player identity field for invite targeting by extending `Assets/Scripts/Networking/PlayerSyncModel.cs` with `userId` realtime property and populating it from auth state in `Assets/Scripts/Networking/PlayerSync.cs`; (2) added new in-world slot component `Assets/Scripts/Environment/Interactables/BoothInviteTargetButton.cs` to represent selectable invite candidates on booth-side UI buttons; (3) upgraded `Assets/Scripts/Environment/Interactables/BoothPortal.cs` to support a runtime-generated invite picker panel (`InvitePickerAnchor/InvitePickerPanel`) with candidate discovery from nearby `PlayerSync` instances, auto-hide behavior, and slot-based user selection flow via `SendInviteFromPicker`; (4) preserved fallback behavior to `_defaultInviteTargetUserId` when no nearby candidates exist; (5) updated `Assets/Editor/EnvironmentGameplayPass.cs` to force portal config for invitation-only + picker defaults (`_requiresInvite=true`, `_useInvitePicker=true`, `_invitePickerSlots=4`, `_inviteCandidateRadius=12`); (6) executed `Drift/Setup/Apply Environment Gameplay Pass`, verified all `Bar_Main` portals (`BoothPortal_A/B/C`) have picker fields enabled, and confirmed compile checks remain clean (`error CS` and `): error` both zero).
- [2026-03-05] CODEX-VR – Scope: pink-square startup regression mitigation for disco floor. **Work completed**: (1) extended `Assets/Scripts/Environment/AudioManager.cs` with `IsMusicPlaying()` helper; (2) updated `Assets/Scripts/Environment/DiscoDanceFloorController.cs` to support idle-safe behavior (`_requireMusicForStrobe`, `_idleTileColor`, `_idleEmission`) so checker tiles remain dark when no track is actively playing and sweep lights stay low-intensity in idle; (3) preserved full neon checker strobe behavior once music is playing; (4) refreshed/compiled and verified console compile filters remain clean (`error CS` and `): error` both zero); (5) captured validation screenshot `Assets/Screenshots/Bar_Main_after_disco_idle_fix.png`.
- [2026-03-05] CODEX-VR – Scope: runtime magenta-block visual failure in `Bar_Main` caused by Normcore Meta XR sample adapter. **Root cause + fix**: (1) user screenshot + console showed `DefaultAvatarEntityAdapter` reflection failure for `Normal.MetaSampleFiles.SampleAvatarEntity` during play; this came from installed package `com.normalvr.normcore.meta.xr` expecting Meta Avatar sample assembly import not present in project; (2) removed `com.normalvr.normcore.meta.xr` from `Packages/manifest.json` to stop the failing adapter path and fall back to base Normcore networking without Meta sample avatar dependency; (3) forced Unity refresh/recompile and executed short play-cycle checks; (4) post-fix console no longer reports `SampleAvatarEntity`/`MetaAvatar` errors and compile filters remain clean (`error CS` and `): error` both zero); (5) captured verification screenshot `Assets/Screenshots/Bar_Main_after_meta_xr_removal_fix.png`.

- [2026-03-06] CLAUDE-Architect – Scope: Batch 6 review + flaky test fix + Batch 7 assignment. **Batch 6 review PASS**: all 20 tasks verified across 3 pillars. Spot-check results: (1) request ID propagation — sound implementation, clean WithContext/FromContext pattern; (2) all 5 backend service_unit_test.go files contain real assertions, not stubs (535 lines, 35+ test cases); (3) RUNBOOK.md (133 lines) and TROUBLESHOOTING.md (147 lines) — complete and well-structured; (4) mobile accessibility fix correctly resolves circular ThemeProvider import; (5) RevenueCat transformIgnorePatterns correct. **Minor flags** (low severity): RUNBOOK.md build command missing `CGO_ENABLED=0` (fix included in Batch 7 Task 1); colorblind filters in accessibility/index.ts are no-op placeholders. **Flaky test fix**: 2 pre-existing mobile test timeouts (CreatorScreen + VerificationStatusScreen) root-caused to heavy navigation barrel `import { ... }` triggering massive transitive module graph — changed to `import type { ... }` in both source files + added 15000ms timeout safety net in both test files. **Validation gates**: Go build ✅, Go tests (30 packages) ✅, Web (50 files, 141 tests) ✅, Mobile (96 suites, 274 tests) ✅ — all clean, zero failures. **Gap analysis completed** for Batch 7 below.

- [2026-03-06] CLAUDE-Architect → CODEX-Refactor – **BATCH 7 ASSIGNMENT (20 tasks)** — Five pillars: (A) backend VR WebSocket integration, (B) mobile screen test coverage, (C) mobile service + component tests, (D) web test coverage, (E) VR EditMode tests. After Batch 7, backend should handle booth signaling end-to-end, mobile untested screens should drop from 33 to ~20, and web page test coverage should expand to all revenue-critical routes.

---

### PILLAR A: Backend VR WebSocket Integration + Doc Fix (3 tasks)

The VR client (`BoothInviteService.cs`) sends/receives four WebSocket message types (`booth_invite`, `booth_invite_response`, `booth_privacy_update`, `booth_host_control`) but the Go backend has zero corresponding handlers. These tasks close the gap.

**Task 1: Booth WebSocket message types + RUNBOOK fix** — In `backend/internal/realtime/messages.go`, add four new `EventType` constants: `EventTypeBoothInvite = "booth_invite"`, `EventTypeBoothInviteResponse = "booth_invite_response"`, `EventTypeBoothPrivacyUpdate = "booth_privacy_update"`, `EventTypeBoothHostControl = "booth_host_control"`. Add corresponding payload structs: `BoothInvitePayload` (fields: `BoothID string`, `InviterID string`, `InviteeID string`), `BoothInviteResponsePayload` (fields: `BoothID string`, `InviterID string`, `InviteeID string`, `Accepted bool`), `BoothPrivacyUpdatePayload` (fields: `BoothID string`, `InviteOnly bool`, `RoomLocked bool`, `CompanionVoiceAllowed bool`, `MaxGuestCount int`), `BoothHostControlPayload` (fields: `BoothID string`, `HostID string`, `Action string` — valid actions: `"lock_room"`, `"unlock_room"`, `"toggle_invite_only"`, `"toggle_companion_voice"`, `"end_party"`). Also fix `backend/RUNBOOK.md` build command — change `GOOS=linux GOARCH=amd64 go build` to `GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build` (critical for production binary).

**Task 2: Booth WebSocket handlers** — In `backend/internal/realtime/client.go`, add case handlers in the message dispatch switch for the four new event types. For `booth_invite`: validate sender is authenticated, validate `InviteeID` is a real user (non-zero UUID), relay the invite message to the target user's WebSocket connection if online (use `hub.SendToUser`). For `booth_invite_response`: validate responder matches `InviteeID`, relay response back to `InviterID`. For `booth_privacy_update`: validate sender is booth host (check `HostID` matches sender), broadcast to all users currently in the booth. For `booth_host_control`: validate sender is host, validate `Action` is one of the allowed values, broadcast to booth participants; if action is `"end_party"`, also send a disconnect signal. Add input validation (reject empty/zero-value fields) and structured logging with request context for each handler.

**Task 3: Booth WebSocket handler tests** — Create `backend/internal/realtime/booth_test.go` with ≥8 test functions: (1) `TestBoothInvite_RelaysToOnlineUser` — mock hub with online target, verify message delivered, (2) `TestBoothInvite_RejectsZeroInviteeID` — verify validation error, (3) `TestBoothInviteResponse_RelaysToInviter` — verify response routing, (4) `TestBoothInviteResponse_RejectsWrongResponder` — sender != InviteeID rejected, (5) `TestBoothPrivacyUpdate_RequiresHost` — non-host sender rejected, (6) `TestBoothPrivacyUpdate_BroadcastsToParticipants` — all booth members receive update, (7) `TestBoothHostControl_ValidatesAction` — invalid action string rejected, (8) `TestBoothHostControl_EndParty_SendsDisconnect` — verify disconnect signal sent to all participants.

---

### PILLAR B: Mobile Screen Test Coverage (6 tasks)

33 screens currently lack tests. These tasks cover the 11 highest-value screens, grouped by domain. Use React Native Testing Library + Jest mocks. Follow patterns in existing screen tests (e.g., `CreatorScreen.test.tsx`, `VerificationStatusScreen.test.tsx`). Each test file should have ≥3 test cases covering: initial render, data loading states, and key user interactions.

**Task 4: Screen tests — core product flows** — Create `mobile/src/__tests__/screens/MatchScreen.test.tsx`, `CompanionScreen.test.tsx`. For MatchScreen: test renders match card, test swipe action triggers API call, test empty state when no matches. For CompanionScreen: test renders companion status, test session controls (start/end), test disconnected state. Mock `services/matching` and `services/couples` APIs.

**Task 5: Screen tests — revenue flows** — Create `mobile/src/__tests__/screens/SubscriptionScreen.test.tsx`, `CheckoutScreen.test.tsx`. For SubscriptionScreen: test renders plan options, test plan selection updates state, test loading state while fetching plans. For CheckoutScreen: test renders order summary, test payment button disabled when incomplete, test success redirect. Mock `services/purchases` and navigation.

**Task 6: Screen tests — communication** — Create `mobile/src/__tests__/screens/CallScreen.test.tsx`, `VideoCallScreen.test.tsx`. For CallScreen: test renders call controls (mute, speaker, end), test connection state display (connecting/connected/ended), test end call triggers cleanup. For VideoCallScreen: test renders video feeds, test camera toggle, test call duration display. Mock `services/webrtc` and `services/callSignaling`.

**Task 7: Screen tests — chat + settings** — Create `mobile/src/__tests__/screens/ChatScreen.test.tsx`, `SettingsScreen.test.tsx`. For ChatScreen: test renders message list, test send message triggers API, test loading older messages on scroll. For SettingsScreen: test renders all setting sections, test toggle switches update state, test navigation to sub-screens. Mock `services/chat` and `store/settingsStore`.

**Task 8: Screen tests — onboarding + safety** — Create `mobile/src/__tests__/screens/ProfileSetupScreen.test.tsx`, `SafetyScreen.test.tsx`. For ProfileSetupScreen: test renders form fields, test validation on required fields, test submit triggers profile creation. For SafetyScreen: test renders safety tools (SOS, block, report), test SOS button triggers emergency flow, test block user confirmation. Mock navigation and `services/safety`.

**Task 9: Screen tests — VR entry** — Create `mobile/src/__tests__/screens/BoothScreen.test.tsx`. Test renders booth list, test booth selection navigates to detail, test invite-only booth shows lock indicator, test empty state when no booths available. Mock `services/vr` or relevant booth API. This is the mobile-side entry point for the VR experience.

---

### PILLAR C: Mobile Service + Component Tests (5 tasks)

**Task 10: Service tests — revenue + legal** — Create `mobile/src/__tests__/services/purchases.test.ts`, `accountDeletion.test.ts`. For purchases: test product listing fetch, test purchase initiation calls RevenueCat, test receipt validation, test restore purchases, test subscription status check. For accountDeletion: test deletion request triggers API call, test local data clearing after deletion, test cancellation flow. Mock `react-native-purchases` and `api/client`.

**Task 11: Service tests — safety + i18n** — Create `mobile/src/__tests__/services/moderation.test.ts`, `i18n.test.ts`. For moderation: test report submission with reason, test block user, test mute user, test report status polling. For i18n: test translation key lookup, test missing key fallback to English, test locale switching, test pluralization rules. Mock `api/client` and `expo-localization`.

**Task 12: Service tests — engagement** — Create `mobile/src/__tests__/services/rewards.test.ts`, `callKeep.test.ts`. For rewards: test reward balance fetch, test claim reward, test cooldown enforcement, test reward history pagination. For callKeep: test incoming call display, test answer call triggers WebRTC setup, test end call cleanup, test call rejection. Mock `react-native-callkeep` and `api/client`.

**Task 13: Component tests (new directory)** — Create `mobile/src/__tests__/components/` directory and add: `ItemCard.test.tsx`, `MessageBubble.test.tsx`. For ItemCard: test renders item name and price, test press triggers onPress callback, test compact mode renders smaller layout, test free item shows "Free" instead of price. For MessageBubble: test renders text message, test renders image message with thumbnail, test sent vs received styling differs, test timestamp display. These are the two most rendered components in the app. Use `@testing-library/react-native` render + query pattern.

**Task 14: IAP migration** — The only TODO in the codebase is in `mobile/src/utils/expo-iap-compat.ts`: "Migrate subscriptionStore.ts to use services/purchases.ts (react-native-purchases)". Update `mobile/src/store/subscriptionStore.ts` to import and call `services/purchases.ts` methods instead of the compat shim. Keep the public store API identical so consumers don't break. After migration, add a deprecation comment to `expo-iap-compat.ts` (do NOT delete it yet — other files may import it). Add/update tests in `mobile/src/__tests__/store/subscriptionStore.test.ts` to verify the store calls `services/purchases` under the hood.

---

### PILLAR D: Web Test Coverage (3 tasks)

Web currently has 50 test files / 141 tests but 12 Next.js pages have no tests. These tasks cover the 6 highest-priority routes. Use React Testing Library + Vitest. Follow existing web test patterns (see `web/src/app/` test files). Mock API calls with MSW or manual mocks.

**Task 15: Web page tests — revenue routes** — Create `web/src/app/checkout/success/page.test.tsx`, `web/src/app/purchases/page.test.tsx`, `web/src/app/purchases/[id]/page.test.tsx`. For checkout/success: test renders success confirmation, test displays order summary, test redirect to home after timeout. For purchases: test renders purchase history list, test empty state, test loading skeleton. For purchases/[id]: test renders purchase details, test receipt download link. Mock fetch/API calls.

**Task 16: Web page tests — interactive routes** — Create `web/src/app/messages/[matchId]/page.test.tsx`, `web/src/app/profile/edit/page.test.tsx`, `web/src/app/store/[id]/page.test.tsx`. For messages/[matchId]: test renders message history, test send message input, test loading state. For profile/edit: test renders form with current values, test validation on required fields, test save triggers API call. For store/[id]: test renders item details, test add-to-cart button, test age-gated content warning. Mock WebSocket client and fetch.

**Task 17: Web lib tests** — Create `web/src/lib/__tests__/ws.test.ts`, `web/src/lib/__tests__/electron.test.ts`, `web/src/lib/__tests__/intiface.test.ts`. For ws: test connection lifecycle (connect/disconnect/reconnect), test message queue while disconnected, test auth token injection in headers. For electron: test IPC channel registration, test message dispatch, test graceful fallback when not in Electron. For intiface: test device connection handshake, test disconnect cleanup, test error state handling. These are pure TypeScript modules — no DOM setup needed.

---

### PILLAR E: VR EditMode Tests (3 tasks)

The VR project has 15+ complex runtime scripts but only 3 EditMode test files. These tasks add coverage for the most critical systems.

**Task 18: VR EditMode tests — booth systems** — Create `vr-dryft/Assets/Tests/EditMode/BoothManagerTests.cs`, `BoothInviteServiceTests.cs`. For BoothManager: test initial state (not locked, invite-only false), test privacy update applies fields correctly, test max guest count enforcement, test end party resets state. For BoothInviteService: test invite message construction format, test invite response routing, test privacy update broadcast structure. Mock `CompanionWebSocket` by creating a test double that records sent messages.

**Task 19: VR EditMode tests — haptics** — Create `vr-dryft/Assets/Tests/EditMode/HapticPatternsTests.cs`, `HapticControllerTests.cs`. For HapticPatterns: test pattern lookup by name returns correct data, test unknown pattern returns null/default, test intensity clamping (values outside 0-1 range), test duration values are positive. For HapticController: test pattern dispatch calls device API, test device enumeration, test disconnect cleanup. Mock `HapticDevice` interface.

**Task 20: VR EditMode tests — marketplace + session** — Create `vr-dryft/Assets/Tests/EditMode/MarketplaceServiceTests.cs`, `InventoryManagerTests.cs`. For MarketplaceService: test item fetch parses response correctly, test purchase validation rejects insufficient balance, test inventory update after purchase. For InventoryManager: test add item to slot, test equipped item tracking, test slot capacity limits, test unequip returns item to inventory. Mock HTTP responses and Unity lifecycle.

---

**Validation for all tasks**: `cd backend && go build ./cmd/dryft-api` must succeed. `cd backend && go test ./... -count=1 -timeout=120s` must pass. `cd web && npm test` must pass (uses Vitest, NOT Jest). `cd mobile && npm test -- --watchAll=false` must pass. VR: Unity must compile with no `error CS` diagnostics. Do NOT break existing tests. Do NOT generate `pnpm-lock.yaml` — this project uses npm. Update AGENTS_COLLAB.md with completion summary when done.

- [2026-03-06] CODEX-Refactor – Scope: Batch 7 completion sweep across `backend/internal/realtime/*`, `backend/RUNBOOK.md`, `web/src/__tests__/setup.tsx`, new web page/lib tests, `mobile/src/__tests__/{screens,services,components}/**`, `mobile/src/__tests__/setup.ts`, and new VR EditMode tests under `vr-dryft/Assets/Tests/EditMode/*.cs`. **Work completed**: (1) finalized backend booth WebSocket integration (new event constants/payloads, handler dispatch + validation + host controls + booth participant fanout/end-party signaling, request-scoped realtime logging wiring) and added comprehensive `backend/internal/realtime/booth_test.go`; (2) completed Batch 7 mobile coverage set for companion/match/subscription/checkout/call/video/chat/settings/profile/safety/booth screens, purchases/account deletion/moderation/i18n/rewards/callKeep services, and `ItemCard`/`MessageBubble` components; (3) fixed newly introduced mobile Jest issues (mock-factory scope restrictions, safe-area test context, alert-confirm modal flow, updated copy assertions, store hook `getState` contract, and RevenueCat/callkeep test harness stubs) so full suite is stable; (4) completed Batch 7 web test targets (checkout success, purchases list/detail, messages by match, profile edit, store detail, ws/electron/intiface) and stabilized web harness (`scrollIntoView` shim + non-ambiguous selectors); (5) added VR EditMode coverage files: `BoothManagerTests.cs`, `BoothInviteServiceTests.cs`, `HapticPatternsTests.cs`, `HapticControllerTests.cs`, `MarketplaceServiceTests.cs`, `InventoryManagerTests.cs`. **Validation**: `cd backend && go build ./cmd/dryft-api` ✅, `cd backend && go test ./... -count=1 -timeout=120s` ✅, `cd web && npm test` ✅ (`59` files / `167` tests), `cd mobile && npm test -- --watchAll=false` ✅ (`112` suites / `332` tests). **Note**: Unity Editor compile diagnostics were not executed in this terminal-only run (no active Unity session attached), but all new VR test scripts are in place under `Assets/Tests/EditMode/`.
- [2026-03-06] CODEX-VR – Scope: Unity-side post-Batch-7 verification run (compile + EditMode tests). **Work completed**: (1) ran Unity compile refresh and found one test compile blocker in `Assets/Tests/EditMode/InventoryManagerTests.cs` (`Object` ambiguity), fixed by qualifying `UnityEngine.Object.DestroyImmediate`; (2) ran Unity EditMode tests and found one runtime test failure in `BoothManagerTests` (`RemoteHostControl_EndParty_ResetsBoothOccupancy`) caused by null `_boothLights` iteration path during emergency exit cleanup; hardened `Assets/Scripts/Environment/BoothManager.cs` with null guards in lighting loops (`InitializeAmbiance`, `SetMoodLighting`, `UpdateLighting`, `SetLightColor`); (3) recompiled and re-ran EditMode suite to green. Validation: Unity compile filters `error CS` / `): error` both zero; Unity EditMode tests `49 total, 49 passed, 0 failed, 0 skipped`.

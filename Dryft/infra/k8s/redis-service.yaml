apiVersion: v1
kind: Service
metadata:
  name: dryft-redis
  namespace: dryft
spec:
  selector:
    app: dryft-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
  type: ClusterIP

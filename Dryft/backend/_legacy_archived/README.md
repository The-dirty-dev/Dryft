# Legacy Node.js/TypeScript Backend

This directory contains the original Node.js/Express/Prisma backend that has been
replaced by the Go backend in the parent directory.

These files are preserved for reference only and are not used in production.

The Go backend (`cmd/dryft-api/`, `internal/`) is the active codebase.
